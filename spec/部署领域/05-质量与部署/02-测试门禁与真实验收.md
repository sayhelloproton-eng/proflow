---
docId: DEPLOYMENT-DOC-05-02
title: 测试门禁与真实验收
docType: testing
authority: normative
lifecycle: active
domain: deployment-governance
subdomain: null
subdomains: []
provides: []
requires: []
contractRefs: []
---

# 测试门禁与真实验收

## D1 — Contract Gate

- `module-contract` TS types compile；
- runtime schemas；
- Result/Error；
- version/compatibility；
- Provides/Requires；
- Config/Lifecycle；
- External Resource version separation。

## D2 — Template + Conformance Gate

至少生成并验证两种不同 Kind：

- library；
- service；
- 再至少一个 external-resource 或 browser-extension。

必须通过 Static/Package/Behavior 三层 Conformance。

## D3 — Platform CLI Offline Gate

使用 fake Modules 验证：

- dependency graph；
- missing dependency；
- config resolution；
- plan immutability；
- plan stale；
- apply resume；
- ACTION_REQUIRED；
- exclusive lock；
- verify history；
- manifest；
- upgrade fail + doctor；
- no automatic rollback。

## D4 — Real External Resource Gate

至少覆盖真实：

- 一个可完全自动管理的 External Resource；
- 一个需要 ACTION_REQUIRED 的 External Resource；
- 一个只可 status/verify、没有 start/stop 的 External Resource。

当前平台优先候选：Dev Tunnel、Chrome、Model API Provider。

## D5 — Cross-domain Deployment Gate

选一套最小真实平台实例，完成：

```text
preflight
→ plan
→ apply
→ services start
→ external resources verified
→ Module verify
→ cross-module verify
→ manifest READY
```

并进行一次 target version upgrade：

```text
old PASS
→ upgrade plan
→ target apply
→ new PASS/FAIL history preserved
```

## D6 — Failure Injection

至少：

- missing config；
- incompatible version；
- external API unavailable；
- Dev Tunnel login required；
- Browser Extension manual action；
- lifecycle failure；
- verify failure；
- stale plan；
- interrupted apply + resume。

## 通过标准

Deployment Domain 不是以“命令退出 0”判定成功，而是以**真实 Module 状态 + Verification + dependency graph + manifest**判定。

---

## 当前正式约束：Conformance 与 E2E 分离

Deployment Conformance 只证明 Module 治理合同，不证明领域业务正确。最终 D5/D6 需要调用各领域 verify 并参加 Cross-domain E2E/Fault Injection；PASS 使用机器可验证 evidence。OpenAI Carrier External Resource 的 Actions/schema/auth/capability 可在真实 E2E 中记录 VERIFIED/PENDING_SPIKE/REJECTED_OR_UNSUPPORTED。

## OpenAI Carrier 实验状态收口

真实 D4/D5 Gate 区分两类：

### 已进入正式合同

```text
openaiFileIdRefs/openaiFileResponse protocol
45s/100k/TLS/443
Action auth
explicit x-openai-isConsequential
no custom headers
File Bridge size/type/header validation
```

### 架构已冻结、真实目标环境证明待 FINAL MANUAL E2E

```text
x-openai-isConsequential:false + Always Allow routine path
Multi-Action Worker Turn
Conversation-native file usage/search
Code Interpreter Context Pack/Patch round-trip
```

这些能力已进入 v1 REUSE/PRIMARY PATH，不再以 `PENDING_SPIKE` 决定“架构是否采用”。Deployment 的职责是在真实目标环境诚实记录 `VERIFIED / DEGRADED / ACTION_REQUIRED / UNSUPPORTED`，并把异常留给对应 Carrier/Owner recovery；不得因为尚未执行最终人工 E2E 就恢复旧的 per-action Browser scheduler、Product pre-Task flow 或 Browser file manager。

其中 **ZIP 是否作为 Context Pack 载体**仍可独立保留 `PENDING_SPIKE`，因为它只是 pack 格式选择，不改变 File Bridge/Artifact/Execution ownership。
### File Bridge P0 Conformance 必测

- [ ] `openaiFileIdRefs` >10、单文件 >10MB、aggregate >50MB 分别得到稳定 typed error；
- [ ] fetch 超过 15s、locator 过期、redirect 到 private/localhost/metadata 地址都 fail-closed，且不触发业务 Effect；
- [ ] malicious filename/path traversal、NUL/control chars 被拒绝；declared MIME 与 detected MIME 冲突按合同处理；
- [ ] relay token 只能 GET 指定 artifact/outputRef，TTL=5min；跨 artifact/token scope 使用被拒绝；
- [ ] GPT-facing request/response 都对最终 JSON 做 `<100,000 chars` hard guard：request 达到/超过上限时在进入 owning Domain 前返回 `OPENAI_ACTION_REQUEST_BUDGET_EXCEEDED`；`openaiFileResponse` inline 临界值前 PASS，达到/超过上限时自动改 URL relay，URL 形态仍超限则返回 `OPENAI_ACTION_RESPONSE_BUDGET_EXCEEDED`；
- [ ] relay response 提供 `Content-Type`/`Content-Disposition`，不泄露本地路径/secret；
- [ ] expired locator / fetch timeout recovery 只重试未产生 business mutation 的 transport，已有 Action/Effect 先查 owner facts/idempotency/result，不 blind replay。

## 2026-08-14 Carrier / Role Readiness 增量 Gate

D4/D5/最终真实 E2E 还必须证明：

- 三个固定 `agentPackageRef/packageName → roleRef → credentialRef` 映射各自独立，credential 不等于 packageName，Browser 不接触 credential；
- Product/Controller-Dev/Test-Ops 的静态 Action surface 与最新 Role contract 一致，Product 不再暴露 New Task `createTask/listRegisteredRoles/getRegisteredRole` 主链；
- routine Actions 的 `x-openai-isConsequential:false` 与 target GPT `Always Allow` 设置可被 setup/doctor/verify 区分，unexpected prompt 只作为 Carrier recovery；
- Actions 不提供稳定 Conversation c-id；Deployment 不伪造 workerRef，Conversation identity 由 Browser Carrier 在 Task Worker creation 时观察；
- File Bridge / Code Interpreter / Web Search readiness 按 Agent Package requirement 校验；exact model id 不作为 Role READY 真源；
- GPT Builder / workspace / auth / privacy policy / domain allowlist 等 Web-only 条件未完成时必须 `ACTION_REQUIRED(_WEB)`，完成后重新观察 reality；
- System Observer 只能读取 Deployment bounded `status/verify/doctor` summary，不能把其 derived assessment 回写为 Deployment READY。
