---
docId: TP-DEPLOYMENT-GOVERNANCE-00
title: Deployment｜领域测试总计划
docType: test-plan
authority: normative
lifecycle: active
domain: deployment-governance
subdomain: null
subdomains: []
boundedContext: deployment-governance
moduleRef: null
provides: []
requires: []
contractRefs: []
testPlanPhase: PRE_IMPLEMENTATION
testPlanStatus: FINAL_FROZEN
sourceBaseline: ProFlow-ProFlow-DDD规范化技术文档-最终冻结基线-20260812.zip
sourceBaselineSha256: 69fdfbac8a5ee36b700bcb10c0b8a9a61f0a5aa3367386cb5bc7e98118a4a875
sourceRefs:
- DEPLOYMENT-DOC-05-02
- DEPLOYMENT-DOC-05-03
testPlanAuthoringOrder: 1
---

# Deployment｜领域测试总计划

## 1. Scope

证明当前 Domain 的冻结 invariant / Public Contract / Module boundary / failure-recovery / real environment assumptions 可以被实现和验证；不在 Test Plan 中新增领域模型。

## 2. Frozen Sources

- [`DEPLOYMENT-DOC-05-02`](../05-质量与部署/02-测试门禁与真实验收.md)
- [`DEPLOYMENT-DOC-05-03`](../05-质量与部署/03-新仓库实施顺序-停止门与非目标.md)

## 3. Module Coverage

| Module | Implementation Wave | Plan |
|---|---|---|
| `module-contract` | Wave 0 | [Module Plan](modules/module-contract.md) |
| `module-template` | Wave 0 | [Module Plan](modules/module-template.md) |
| `deployment-conformance` | Wave 0 | [Module Plan](modules/deployment-conformance.md) |
| `platform-cli` | Wave 6 | [Module Plan](modules/platform-cli.md) |
| `module-skill` | Wave 6 | [Module Plan](modules/module-skill.md) |

## 4. Domain Gates

- D1 Contract Gate：Module Contract TS/runtime schema/version/config/lifecycle/external-resource separation。
- D2 Template + Conformance：至少 library + service + external-resource/browser-extension profile，C1/C2/C3。
- D3 Platform CLI Offline：fake graph、missing dependency、plan/apply/ACTION_REQUIRED/resume/verify/manifest/upgrade。
- D4 Real External Resource：可自动管理、ACTION_REQUIRED、status/verify-only 三类。
- D5 Cross-domain Deployment：preflight→plan→apply→services/external resources→verify→manifest READY + upgrade history。
- D6 Failure Injection：missing config/version/resource unavailable/manual action/lifecycle/verify/stale plan/interrupted apply。

## 5. Domain Test Responsibility

- Domain Behavior：只证明本领域状态/invariant/idempotency/concurrency/error/recovery。
- Contract：Provider + Consumer 双侧，runtime schema 不能只靠 TS type。
- Integration：尽量使用真实 storage/serializer/schema/adapter；昂贵外部依赖才 fake。
- Real Acceptance：如果被验收能力就是 Browser/Carrier/Local Effect/Model/External Resource 本身，则不能只 Mock。
- Conformance：只证明平台治理结构，不替代 Domain PASS。

## 6. STOP

如果测试计划要求的行为在冻结规范中无依据，标记 SPEC_GAP；如果会阻断正确实现，则在 Codex 开工前进入正式设计变更，而不是在测试或代码里补语义。

## 7. 2026-08-14 Carrier / Journey Test Addendum

D4/D5/D6 追加：

- 三固定 Agent Packages 的 roleRef/credential mapping、static Action schema 与 native capability requirements 可被 verify/doctor；
- Product GPT New Task旧 Action surface被拒绝/不发布；
- routine Action consequential/Always Allow 与 Execution safety Approval 严格分离；
- Web-only setup 未完成时 `ACTION_REQUIRED(_WEB)`，完成后必须 re-observe，不能凭人点击“完成”直接 READY；
- exact model id 变化但 required behavior/capability仍满足时不应错误判 UNREADY；反之 model id看似正确但 behavior/auth/schema失败必须拒绝 READY；
- Actions 无稳定 c-id，Deployment 不生成/验证 Task workerRef；Browser Carrier owns Conversation observation；
- System Observer 对 Deployment 只读 bounded summary，没有 READY write authority。
