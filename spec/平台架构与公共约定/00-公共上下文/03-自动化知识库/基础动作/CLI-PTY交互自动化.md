# CLI / PTY 交互自动化｜兼容入口

> 本文件仅保留旧路径兼容，不再维护通用 CLI/PTTY/Expect 自动化 SOP。

ChatGPT Chat 本机 CLI/PTTY Acceptance 自动化唯一规范入口：

`/Users/agent/Desktop/proton-workspace/skills/chat-local-acceptance-automation-protocol/SKILL.md`

Prompt observation、single-answer、PTY/session recovery、timeout/partial output、Browser-mediated auth 与 do-not-repeat 等通用语义只由该 Skill 维护。

ProFlow 特有的 platform CLI 产品命令、prompt contract、正式 helper identity 与业务状态仍由 `../包能力/platform-cli.md`、仓库真实脚本和正式 Spec 拥有。

旧文件详细方法由 Git history / `90-历史记录` 保存，不得作为第二套 active CLI automation authority。
