# Browser / System UI 自动化｜兼容入口

> 本文件仅保留 ProFlow 旧路径兼容，不再维护 Browser/UI/AX/Playwright 的通用自动化 SOP。

ChatGPT Chat 本机 Browser/UI Acceptance 自动化唯一规范入口：

`/Users/agent/Desktop/proton-workspace/skills/chat-local-acceptance-automation-protocol/SKILL.md`

按该 Skill 的 conditional loading 使用其 Browser/System UI reference。Reality-first、EYES、observe-vs-mutate、identity、controlled context、Browser owner、privileged UI、action primitive failure、recovery 等通用语义均只在那里维护。

ProFlow 特有的 **Execution Browser Extension 产品 identity、版本/adoption、pairing、Tasks surface、Carrier/Worker/Conversation 合同与产品 PASS 条件** 仍由 `../包能力/execution-browser-extension.md`、正式 Spec 和当前 Flow/CURRENT 拥有。

旧文件中的 token/runtime 版本、Playwright connect 细节、AX selector、固定工作集和通用恢复方法不再是 active authority；需要历史原因时查 Git history / `90-历史记录`。
