# 执行纪律与工具规则

## 1. 验收阶段三方分工

### 验证总纲总控（默认由当前主 Chat 承担）

负责：阶段状态、验收目标、PASS/FAIL 条件、最短真实 Journey、failure 分类、root owner、整改边界、Evidence 审查、STOP/GO 与是否允许进入下一 Real。

总控的默认身份是 **验收主持者**，不是持续开发者。发现失败时先分类和裁决；需要工程整改时，优先把已经锁定的最小整改任务交给 Codex / 领域执行者。只有用户明确要求总控直接修复时，才由总控进入代码修改。

### Codex / 领域执行者

负责：按总控冻结的范围执行验证或整改、收集 evidence、CodeGraph 定位、实现最小 fix、跑 targeted regression、返回可复核结果。

执行者无权自行扩大验收范围、重开已 PASS 阶段、修改 Frozen Architecture/Owner/Contract，或仅凭测试全绿宣布 Real PASS。

### 用户

只承担不可替代的人类/外部动作与必要授权，例如真实登录/授权、Custom GPT Web 编辑、Chrome/Extension 操作、Allow/Deny、手机模型前台、发布授权。

机器能够自动确定或 producer-owned 的事实不得反问用户。

## 2. 验收默认先只读，整改必须过门

真实失败出现后先按 `04-测试验证与验收方法.md` 分类。

- A / B：继续完成真实前置，不进入代码整改。
- C / D：总控锁定 root owner、允许文件/Package 范围、回归测试 seam、原场景重放要求后，才允许工程修改。
- E：立即 STOP，进入正式 Spec/Contract/Design Change，不允许执行者自行补设计。

不得采用“看到错误 → 先改代码 → 再解释为什么”的顺序。

## 3. 大任务落盘规则

进入工程整改后：大任务先拆大批，每个大批继续拆小批；小批默认修改 2～4 个文件，接近 5 个文件或完成一个独立 root cause 即 STOP + checkpoint。

checkpoint 必须真实落盘；恢复包必须是累计 Overlay，不是只含本小批 delta。后续修改基于当前 working tree，禁止反复覆盖旧 ZIP。

验收阶段纯只读执行不为了形式制造 Overlay；只有真正修改仓库时才启用整改 checkpoint 规则。

## 4. 本机与工具规则

- 时间优先；能用短确定性命令验证就不要长时间猜测。
- 长命令必须短启动 + poll；未知状态的 publish/install/deploy 不得盲目重试。
- 代码结构问题优先 CodeGraph → symbol/call chain/blast radius → 再读锁定源码。
- 不为了“看得更全”无目的扫描整个仓库。
- 用户已经给出的事实或本机可自动读取的事实，不重复询问。

## 5. 修改后的机械前置

只有进入获准的工程整改后，正式 gate 前固定：

```text
pnpm format
→ changed-files scoped pnpm exec biome check --write <files>
→ targeted test / typecheck / lint
→ 必要的正式 gate
→ 原真实验收场景重放
```

结构 gate 证明实现质量；原场景重放证明验收 blocker 是否真正关闭，两者不能互相替代。

## 6. 发布纪律

发布前先确认 branch / HEAD / working tree / release status；长 publish 状态 UNKNOWN 时先恢复权威状态，禁止重复发布。

总控负责决定“是否已经 READY_FOR_REPUBLISH”；实际 version/publish 必须遵守用户授权与 release policy。

Fresh Registry 最终证明必须使用真正新的 Workspace / cache，不能拿旧安装物、local link、repo source 或 tarball 冒充 Registry reality。