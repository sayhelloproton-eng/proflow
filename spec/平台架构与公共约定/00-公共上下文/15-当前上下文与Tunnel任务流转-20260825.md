# 当前上下文与 Tunnel 任务流转｜2026-08-25

仓库：`/Users/agent/Desktop/proton-workspace/repos/proflow`
工作区：`/Users/agent/Desktop/proton-workspace`

## 1. 当前最高层状态

```text
ARCHITECTURE = FROZEN
REAL_1 = PASS
REAL_2 = PASS
REAL2_PROVISIONING_GO = YES
READY_FOR_REAL_3 = YES
CURRENT_STAGE = REAL_3
PHASE3_FINAL_GO = NO
```

Real-2 已完成 Narrow Audit、文档同步并正式冻结；不得因 Tunnel 或 setup 自动化重开。
Real-2 当前真源优先读取：`13-Real2-最终冻结与Real3交接.md`。

本文同时承担“当前上下文 + Tunnel 独立任务流转 + 配置自动化进度 + Real-3 下一步顺序”，不再创建同功能的 16/17 等重复交接文件。
## 2. 配置自动化当前进度

`chatgpt-carrier = DONE`：已删除人工 GPT URL / capability confirmation；Deployment 只观察 ChatGPT Web 外部可用性，不维护第二份 Role/Auth/Capability truth。

```text
f81b77b refactor(setup): automate ChatGPT carrier observation
e798db4 refactor(setup): remove manual ChatGPT carrier configuration
```

包内测试 `11/11 PASS`，typecheck PASS，真实 ChatGPT Web probe READY。父工作区仍安装旧 Registry 包，统一 release/install 前 `platform status` 可能继续显示旧 ACTION_REQUIRED；不得把它误判为源码回归。

`model-provider-api = BOUNDARY_CONFIRMED`：当前没有正式 mDNS / Bonjour / producer-owned endpoint discovery，`providerBaseUrl` 暂时属于真正外部部署输入，禁止从历史 LAN IP 或盲扫局域网猜测。

`model-runtime = DEFER_TO_MODEL_DOMAIN`：`/models` 可发现候选，但 deployment `profiles()` 尚缺 producer-owned capability profile；FAST/REASON 不能按模型名、历史 ID 或固定映射猜测。应在模型领域以真实 bounded capability probe 闭环后再自动选型。

`execution-runtime = PENDING_SMALL_CLEANUP`：旧 SETUP 仍把 loaded Chrome Extension ID 写成人工输入；当前 ID 已由 hello/pairing/heartbeat 机器发现，应删除该人工合同。

`execution-browser-extension` 继续保持 Extension ID / Bridge / heartbeat 自动发现；普通 Chrome 首次安装仍尊重浏览器安全边界。
## 3. Tunnel 第一轮结论：未冻结

第一次独立调研返回：

```text
A. NOT_COMPATIBLE
OPENAI_TUNNEL_GENERAL_HTTP_INGRESS = NO
OPENAI_TUNNEL_GPT_ACTIONS_COMPATIBLE = NO
SOURCE_CODE_MODIFICATIONS = 0
SECOND_PHASE_ENTERED = NO
```

其依据是：Secure MCP Tunnel 面向 OpenAI 产品 ↔ 私有 MCP Server，`tunnel-client runtimes connect` 目标为 MCP command/server；未证明存在可交给 Custom GPT Actions `servers.url` 的通用公网 HTTPS reverse-proxy URL。

用户随后明确要求再独立调研一次，因为 Microsoft Dev Tunnel 与 OpenAI Secure MCP Tunnel 都属于 Tunnel，需要排除第一次调研遗漏高级/隐藏/新能力的可能性。

```text
TUNNEL_FIRST_REVIEW = NOT_COMPATIBLE
TUNNEL_FIRST_REVIEW_FROZEN = NO
TUNNEL_SECOND_REVIEW_REQUIRED = YES
SOURCE_CODE_MODIFICATIONS = 0
```

第二轮完成前，不得把 NOT_COMPATIBLE 写成最终冻结架构事实。
## 4. Tunnel 第二轮独立调研提示词

```text
接管 ProFlow 的 OpenAI Secure MCP Tunnel 第二轮独立可行性审计。

仓库：/Users/agent/Desktop/proton-workspace/repos/proflow

先完整读取：
spec/平台架构与公共约定/00-公共上下文/13-Real2-最终冻结与Real3交接.md
spec/平台架构与公共约定/00-公共上下文/14-配置自动化与OpenAI-Secure-MCP-Tunnel后续.md
spec/平台架构与公共约定/00-公共上下文/15-当前上下文与Tunnel任务流转-20260825.md

这是第二轮独立审计。第一轮曾给出 NOT_COMPATIBLE，但该结论未冻结。不要继承第一轮推理，也不要为了证明第一轮对或错而选择证据；从零重新调查。

本轮只调研 Tunnel，不处理其它 setup 自动化，不修改 ProFlow 源码/规范/测试/配置，不 publish。

重点不是重复解释“MCP 与 HTTP 不同”，而是专门排除这些可能遗漏：
1. OpenAI Secure MCP Tunnel 最新官方文档是否存在 general HTTP ingress / reverse proxy 模式；
2. Harpoon / advanced mode 是否能把任意本地 HTTP REST service 直接暴露给 GPT Actions，而不是仅作为 MCP server 内部 allowlist target；
3. hosted tunnel endpoint 是否存在稳定、可直接请求的 HTTPS URL，以及该 URL 的 path/method/auth contract；
4. HTTP MCP transport 与普通 HTTP reverse proxy 是否存在官方桥接模式；
5. ChatGPT developer mode / Apps / Plugins / Actions 是否有 tunnel_id 与 Custom GPT Actions OpenAPI servers.url 的直接集成；
6. tunnel-client 0.0.11 是否存在 help 未明显展示的 target/provider 模式、profile 字段或实验入口；
7. 官方示例、SDK、CLI 源码/发布说明里是否存在“non-MCP local HTTP service → OpenAI-hosted HTTPS ingress”证据。
```
```text
证据优先级固定：OpenAI 最新官方文档/发布说明/API/CLI 文档 → 本机 tunnel-client help/quickstart/profile/runtime 行为 → 必要时最小无副作用实验。社区讨论只能作为线索，不能作为最终合同证据。

必须明确区分：
- 底层使用 HTTP/HTTPS 传输；
- MCP 的 HTTP transport；
- 通用公网 HTTP ingress / reverse proxy。
不能因为前两项存在就推导第三项存在，也不能因为产品强调 private MCP 就提前断言第三项一定不存在。

当前 ProFlow 插槽必须原样满足：
local agent-gateway → stable public HTTPS base URL → Custom GPT Actions OpenAPI servers.url → arbitrary existing Gateway REST path/method。

第二轮最终只能给：
A. CONFIRMED_NOT_COMPATIBLE：两轮独立证据一致，可以冻结 NOT_COMPATIBLE；源码零修改。
B. FIRST_REVIEW_OVERTURNED：找到官方/实测 general HTTP ingress 证据；先只给兼容性证据与最小替换方案，STOP 等总控决定是否进入实现。
C. INCONCLUSIVE：证据不足，明确缺哪项，不猜。

禁止为了让 Tunnel 兼容而把 Frozen Custom GPT Actions + HTTP Gateway 静默改成 MCP Gateway。
绝不读取、打印或提交 Admin Key、Runtime API Key、Tunnel token、Role credential。
```

## 5. 配置自动化之后的 Real-3 顺序

Real-3 下一条主链从 J1 开始：

```text
J1 Task Orchestration + Browser Extension
PENDING Task → 创建/观察/绑定 Product/Dev/Test 三个真实 Worker Conversation。

J2 Task Orchestration
Requirement + bindings READY → 人工确认 → startTask → ACTIVE。

J3 Owner Projection + Browser Extension Observer/Carrier
Node READY → RESTORE/WAKE 已绑定 Conversation → delivery → Worker 自己 startNode。

J4 Agent/Gateway + Browser Carrier + Execution/File
同一 Worker Turn 支持 0..N Actions；验证 Context Pack / File Bridge / Code Interpreter / Patch handoff。
```

Model Runtime 不是 J1～J4 正常调度者；正常推进 deterministic first。FAST/REASON 手机模型与 Model E2E 主要属于后续 Real-5。