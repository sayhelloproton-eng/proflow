# Real-3 全量整改实施、发布与 Fresh 回放进度（2026-08-29）

> 状态：当前最新执行进度入口。
> 上游完整问题登记：`16-Fresh-Workspace真实人工验收与新Chat交接-20260829.md` 第 10 节 `P1～P18`。
> 本文只续接实施、发布和回放进度；不得用本文省略或覆盖 `16` 中的原始问题、D1～D5 产品裁决与两个额外 P0。

## 1. 当前授权与边界

用户已批准 D1～D5 与 P1～P18 的全量整改计划，并在候选准备完成后明确要求“继续执行，同时把上下文和进度落盘”。本轮据此继续：

1. 真实 npm Registry 发布；
2. Registry 发布后的独立 Fresh Workspace 回放；
3. 持续更新本文中的真实证据和 blocker。

本授权仍不包含：

- `git push`；
- 删除任何远端 Dev Tunnel；
- 自动清理历史 orphan；
- 修改 Frozen Architecture / 23 active modules 定义；
- 在真实证据未完成前宣布 `PHASE3_FINAL_GO = YES`。

## 2. 已实施的产品范围

实施对应已批准的统一方案：

- D1：Platform 是唯一专业交互外壳，各 Module 保留 setup/observe/READY ownership；
- D2：用户可感知顺序固定为 Browser → Tunnel → Provider，机器依赖按图静默闭环；
- D3：Provider Base URL、可选 API Key、真实库存、FAST/THINK 由 Platform 专业交互完成；
- D4：Dev Tunnel 使用 Microsoft CLI 能力、系统/包管 CLI resolver、PENDING ownership 与幂等恢复；
- D5：Browser Developer Mode gate、隐藏目录剪贴板引导、自动 pairing/heartbeat 观察及 pre-start 容错；
- P1/P2/P4/P9/P12/P18：install/status/docs/targeted/pure-read 等诊断与呈现收口。

关键实现提交：

```text
5517947 feat(platform): guide setup orchestration and model input
fe5fc82 feat(tunnel): add recoverable managed CLI provisioning
b0b87ab feat(browser): guide hidden extension installation
e97ecd8 fix(status): report only missing gateway dependencies
ca12fa7 test(governance): refresh executable inventories
```

发布准备提交：

```text
06bbd98 chore(release): plan real3 setup candidate
d7a70ed release: version real3 setup candidate
```

`06bbd98` 同时修复版本同步工具对“非模块工具包”的错误假设：完整缺少 Module manifest + descriptor 的工具包跳过；只缺其中一个继续 fail closed。

## 3. 候选版本矩阵

```text
@tomflow/proflow-platform-cli                 0.1.40
@tomflow/proflow-agent-gateway                0.1.15
@tomflow/proflow-deployment-conformance       0.1.13
@tomflow/proflow-dev-tunnel                   0.1.17
@tomflow/proflow-devtunnel-cli                0.1.0   # 首次发布，非 active Module
@tomflow/proflow-execution-browser-extension  0.1.15
@tomflow/proflow-model-provider-api           0.1.14
@tomflow/proflow-model-runtime                0.1.17
```

首轮发布为 Platform `0.1.39`；真实 Fresh install 发现非模块工具包 discovery 缺口后，Platform 单独修复并发布 `0.1.40`。其余 16 个包没有本轮代码变更，不重复发布。

## 4. 发布前证据

候选 HEAD：

```text
d7a70ed9cb1fa2d6535b567419efeda678fc05a4
```

发布前机械事实：

- source repo working tree：clean；
- npm identity：`tomflow`；
- 上述 8 个精确版本在 Registry：全部 ABSENT；
- `pnpm release:sync:check`：PASS，24 个 package 目录；
- `pnpm check`：PASS；
- executable tests：622/622；
- architecture：PASS，23 active modules；
- publishability：PASS，24 package tarballs、全部公开 JS exports、14 binaries 经隔离安装消费；
- `pnpm release:publish:dry-run`：只选择上述 8 个版本，PASS。

## 5. 已完成的受控人工模拟

在独立临时工作区 `/tmp/proflow-fresh-e2e.8H3BeF` 完成当前源码候选模拟：

- `platform install`：23/23；
- 只生成 pnpm lock，没有 npm/pnpm 双管理器污染；
- `platform docs` 默认索引与 filter：PASS；
- agent-gateway 只报告真实缺失的 dev-tunnel：PASS；
- 实际 TTY Provider Base URL、确认、FAST/THINK 选择：PASS；
- Provider/Runtime 真实探测：PASS；
- 根 prerequisite 缺失时 `platform start` fail closed，0 fake process。

真实 Browser 与真实 Tunnel 未在模拟阶段执行，避免未经发布便创建远端资源或继续制造 orphan。

## 6. 产品根工作区当前事实

`/Users/agent/Desktop/proton-workspace` 已清理并 reset 到 tracked truth：

- Git clean；
- `package-lock.json` ABSENT；
- `node_modules/.ignored/@tomflow` ABSENT；
- `node_modules` ABSENT；
- tracked `package.json + pnpm-lock.yaml + pnpm-workspace.yaml` PRESENT。

已确认旧 tracked lock 指向缺失的本地 Real-3 candidate tarballs，且 integrity 与同版本 Registry 包不同；因此不得更新 checksum 假装恢复。新版本发布成功后，应从真实 Registry 生成新的单 package-manager lock，再做 Fresh 回放。

## 7. 远端资源保护边界

不得删除或误用：

```text
swift-fog-skzzjw8.jpe1
swift-chair-28k7rl1.jpe1
quick-chair-9rv1scq.jpe1
ai-agent-platform-mvp.eun1
```

前三个是已知 `portCount=0` 的历史半成品；最后一个是历史资源。只有新实现证明 retry 不再产生 orphan 后，才可另行请求精确删除前三个的授权。

## 8. 当前执行队列

```text
1. publish 上述 8 个精确版本
2. Registry readback：version / dist.integrity / availability
3. 独立 Fresh Workspace：platform install
4. platform setup：Browser → Tunnel → Provider + FAST/THINK
5. platform start → status
6. setup retry 幂等与 Tunnel orphan 数量差分
7. 记录 P1～P18 回归结果
8. 未获授权前不清 orphan、不 push
9. 最后才进行 PHASE3_FINAL_GO 裁决
```

## 9. 状态更新规则

本文中的“计划执行”不等于 PASS。每一步完成后必须写入精确结果；任何 Registry、Browser、Tunnel、Provider 或 runtime 失败均保留为 blocker，禁止以本地测试替代真实外部证据。

## 10. 真实发布执行快照

用户明确“继续执行”后，已执行 `pnpm release:publish`。命令对上述 8 个精确版本均返回 `Published package`；随后逐包做公共 Registry readback，而不是仅相信 publish 退出码。

当前 readback：

```text
@tomflow/proflow-platform-cli@0.1.39                 PRESENT
@tomflow/proflow-agent-gateway@0.1.15                PRESENT
@tomflow/proflow-deployment-conformance@0.1.13       PRESENT
@tomflow/proflow-dev-tunnel@0.1.17                   PRESENT
@tomflow/proflow-execution-browser-extension@0.1.15  PRESENT
@tomflow/proflow-model-provider-api@0.1.14           PRESENT
@tomflow/proflow-model-runtime@0.1.17                PRESENT
@tomflow/proflow-devtunnel-cli@0.1.0                 PACKUMENT_PENDING
```

`proflow-devtunnel-cli@0.1.0` 当前外部证据：

- publish 返回成功；
- `npm dist-tag ls` 返回 `latest: 0.1.0`；
- 精确 tarball URL 返回 HTTP 200；
- 再次发布同一版本被 Registry 拒绝为 `previously published versions: 0.1.0`；
- `npm access get status` 返回 `public`；
- 但公共 package metadata/packument 仍返回 HTTP 404。

当前判断：Registry 首次 scoped package 的 metadata 与 tarball/dist-tag 传播暂时不一致。Fresh package-manager resolution 依赖 packument，因此在它变为 PRESENT 前不得声称 Registry Fresh install PASS，也不得为了绕过传播问题擅自升到 `0.1.1` 或使用 tarball 直链代替真实 Registry 路径。

## 11. 发布后 Fresh install 新发现与整改

独立真实 Registry 工作区：

```text
/tmp/proflow-registry-fresh.Vc40OF
```

从 Registry 安装 `platform-cli@0.1.39` 成功，`platform -v = 0.1.39`。随后执行真实 `platform install`，Registry 搜索实际返回 24 个 `@tomflow/proflow-*` 候选，其中包含 D4 新增且明确“不计入 23 active modules”的 `@tomflow/proflow-devtunnel-cli`。

原实现把所有前缀匹配包都当成 Module 校验，因此出现：

```text
REGISTRY_RESPONSE_INVALID
registry contains rejected ProFlow packages:
@tomflow/proflow-devtunnel-cli:METADATA_INVALID
```

根因：Platform Registry discovery 只有 active Module 与 retired package 两类，没有“受治理非模块工具包”分类。首次工具包发布把该缺口变成真实 Fresh install blocker。

最小修复：

- 增加显式 `PRO_FLOW_TOOL_PACKAGES` allowlist；
- 当前只包含 `@tomflow/proflow-devtunnel-cli`；
- 工具包不进入 Module manifest view、不计入 23 modules、不显示给普通用户；
- 未知或损坏的 ProFlow Module 仍然 fail closed；
- 不采用任意包自称 `proflow.tool=true` 即被静默信任的宽松策略。

实现提交：

```text
355b2bd fix(platform): ignore governed registry tools
```

回归证据：

- 新测试先 RED：工具包被当成 Module，`result.tools` 缺失；
- 最小实现后 targeted tests：2/2 PASS；
- platform-cli package tests：71/71 PASS；
- platform-cli typecheck：PASS。

该修复需要把 Platform 从 `0.1.39` 升至 `0.1.40` 并重新发布；其余 7 个已发布版本不重复升版。

## 12. Platform 0.1.40 发布与全新 Registry Fresh 回放

Platform tool discovery 修复已完成完整门禁：

- executable tests：623/623；
- governance：0 errors；
- build/lint/typecheck：PASS；
- architecture：PASS，23 active modules；
- publishability：PASS，24 package tarballs、14 binaries；
- publish dry-run：只选择 `@tomflow/proflow-platform-cli@0.1.40`；
- Registry publish：PASS；
- 公共 Registry readback：`platform-cli@0.1.40` 与 `devtunnel-cli@0.1.0` 均 PRESENT 且具有 `dist.integrity`。

版本提交：

```text
b1ce58f release: version platform registry tool fix
```

全新、未复用旧 lock/node_modules 的工作区：

```text
/tmp/proflow-registry-fresh-0140.NsYAhy
```

真实结果：

```text
platform -v                         = 0.1.40
Registry 候选模块                   = 23/23
本地安装物验证                      = 23/23
platform install                    = PASS
package-lock.json                   = ABSENT
pnpm-lock.yaml                      = PRESENT
node_modules/.ignored/@tomflow      = ABSENT
package.json ProFlow dependencies   = 23
platform docs                       = 23 项索引，不刷全文
初始 status                         = 15 ready / 3 root / 5 downstream / 0 failed / 0 processes
```

这证明 `proflow-devtunnel-cli` 已作为 `dev-tunnel` 的内部依赖安装，但没有成为第 24 个 Module，也没有暴露给普通用户。

## 13. 当前真实 Browser setup gate

已在上述全新 Registry workspace 启动真实全量 `platform setup`：

1. 自动模块跳过/闭环；
2. 第一个用户可感知步骤是 Browser；
3. Platform 只显示一个 Next/Enter 确认；
4. 确认后自动打开 `chrome://extensions/`；
5. Extension payload 已生成；
6. 正确绝对路径已自动写入 macOS 剪贴板：

```text
/private/tmp/proflow-registry-fresh-0140.NsYAhy/.proflow/deployment/browser-extension/execution-browser-extension
```

当前 setup 正在等待本次 Extension pairing + heartbeat，没有继续执行 Tunnel，证明 D1/D2 的首个人类动作 STOP 与 Browser-first 顺序在真实 Registry 工作区成立。

Chrome 内部 `chrome://extensions/` 页面不能由 ChatGPT Chrome 控制扩展接管，且安装浏览器扩展属于必须由用户在动作发生时确认的操作。因此当前唯一人类动作是：开启 Developer Mode，点击 Load unpacked，在目录选择器按 `Cmd+Shift+G`、`Cmd+V`、Enter、Select。完成后 Platform 应自动观察 READY 并进入下一步骤。

## 14. 用户外出后的目标调整与前置交互验收

用户明确当前在外面，无法执行 Browser/Tunnel 等授权动作。执行目标因此调整为：

```text
只验证人工动作之前的顺序、提示、取消、STOP 和零副作用
→ 保留 Fresh Workspace
→ 等用户回来后再做真实人工授权
```

当前禁止继续：

- 不代替用户加载 Chrome Extension；
- 不开始 GitHub Tunnel 登录；
- 不创建/复用/host Tunnel；
- 不写 Provider URL、API Key 或模型映射；
- 不执行 `platform start`；
- 不宣布 Real-3 Final PASS。

### 14.1 Browser 前置交互

真实全量 setup 已验证：

- 自动模块先静默跳过/闭环；
- 第一个用户步骤固定为 Browser；
- 提示为“下一步将打开 Chrome，并引导你加载浏览器扩展。按 Enter 继续”；
- Enter 后打开 `chrome://extensions/`；
- 输出 Developer Mode gate；
- 输出 `Load unpacked → Cmd+Shift+G → Cmd+V → Enter → Select`；
- 剪贴板内容是当前 Fresh Workspace 的正确绝对路径；
- pairing 等待期间没有继续进入 Tunnel；
- 无人工加载后最终 fail closed 为 `PAIRING_TIMEOUT`，没有伪造 READY。

当前 Browser setup 已结束，不再后台等待。Extension payload 保留，用户回来后可幂等重跑。

### 14.2 Tunnel 前置交互

targeted setup 真实显示：

```text
下一步将检查远程连接；如需登录，会打开 GitHub 授权。按 Enter 继续。
Yes / No
```

选择 `No` 后：

```text
INVALID_REQUEST
配置已取消
```

取消后机械确认：

- `.proflow/runtime/external-resources/dev-tunnel/setup.json` ABSENT；
- system Dev Tunnel CLI 仍为 `Login required`；
- 没有 setup 进程；
- 没有执行 create/port create/host/delete；
- 没有进入远端资源 mutation。

### 14.3 Provider 前置交互

targeted setup 首先显示：

```text
下一步配置 OpenAI-compatible 模型服务地址。按 Enter 继续。
```

进入输入页后显示：

```text
OpenAI-compatible 服务 Base URL
例如 https://api.deepseek.com 或 http://192.168.0.108:8080/v1
```

在未输入 URL 时取消，输出：

```text
已取消配置，没有继续执行后续步骤。
```

并保留可操作说明：填写的是整个服务 Base URL，不是网页、单模型地址或 `/chat/completions`。取消后确认 Provider 文件 ABSENT、credential 文件 ABSENT，没有 secret 或部分配置落盘。

### 14.4 当前已验证与待人工验证分界

已经完成：

- Registry 发布与 readback；
- Fresh install 23/23；
- 单 pnpm package-manager hygiene；
- install/status/docs 输出；
- full setup Browser-first 与首个人工动作 STOP；
- Browser 剪贴板/隐藏路径指导；
- Tunnel 授权前确认与取消零副作用；
- Provider URL 说明、示例与取消零副作用；
- 自动测试 623/623、架构、可发布性。

等待用户回来后人工验证：

1. 真实加载当前 Fresh Workspace Extension；
2. pairing + heartbeat READY；
3. setup 自动进入 Tunnel；
4. 真实 GitHub 授权；
5. managed/system CLI resolver、create/reuse、port、host、URL probe；
6. retry 不产生新 orphan；
7. Provider URL/API Key（如需要）、真实库存、FAST/THINK；
8. `platform start → status`；
9. 全流程幂等重跑；
10. Phase 3 Final GO 裁决。

恢复入口固定为：

```text
cd /tmp/proflow-registry-fresh-0140.NsYAhy
./node_modules/.bin/platform setup
```

但 `/tmp` 可能被系统清理；恢复前必须先确认目录存在、`platform -v = 0.1.40`、单 pnpm lock 与 source/Registry 当前状态。若目录不存在，重新创建独立 Fresh Workspace，不恢复旧污染工作区。

## 15. 根产品工作区重新安装后的补充人工验收（2026-08-29）

用户将 `/Users/agent/Desktop/proton-workspace` 根工作区恢复到 `origin/main` 基线后，使用真实 Registry `platform-cli@0.1.40` 重新执行了未安装、安装后未配置、直接启动等路径。本节只登记真实观察与后续整改要求，不代表已授权修改实现。

### 15.1 未安装状态与未知命令

在根工作区尚未安装任何 ProFlow 模块时，真实执行 `platform status` 得到：

```text
已读取 0 个模块状态
0 配置已完成 · 0 根阻塞 · 0 下游等待 · 0 失败 · 0 个真实进程运行中
PLATFORM_READY=YES
```

该结果是确认的假 READY。当前 renderer 只判断 `ACTION_REQUIRED / BLOCKED / FAILED` 均为零，没有对 `modules.length === 0` 做未安装门禁。正确产品语义必须是：

```text
尚未安装 ProFlow
下一步：platform install
PLATFORM_READY=NO
```

用户还真实输入了拼写错误 `platform steup`。当前 CLI 输出 `INVALID_REQUEST / unknown command steup` 和整份帮助，没有提供最接近命令提示。该输入本身不是 setup 实现失败，但交互应改为中性且可恢复的：

```text
未知命令 “steup”
你是否想运行：platform setup？
```

### 15.2 真实 Registry install 文件与内容核对

真实 `platform install` 成功，机械核对结果：

```text
global platform                         = 0.1.40
workspace local platform                 = 0.1.40
package.json direct ProFlow dependencies = 23
pnpm list --depth 0 ProFlow packages     = 23
package-lock.json                        = ABSENT
pnpm-lock.yaml                           = PRESENT
node_modules/.ignored/@tomflow           = ABSENT
Dev Tunnel external ownership file       = ABSENT
真实 ProFlow 进程                         = 0
```

安装后的预期变更包括：

- `package.json` 写入 23 个精确版本模块；
- `pnpm-lock.yaml` 锁定 23 个模块及其支持依赖；
- `pnpm-workspace.yaml` 写入本轮新发布版本的 `minimumReleaseAgeExclude`；
- `node_modules/@tomflow` 存在 23 个顶层模块；
- `@tomflow/proflow-devtunnel-cli@0.1.0` 作为非模块工具依赖存在于 lock，不计入 23 active modules；
- `.proflow` 已生成 workspace metadata、Browser Extension payload、SQLite、本地 shared facts 和 owner-only token。

所有实际 token 文件均为 `0600`；验收只记录 PRESENT/ABSENT 和权限，没有输出 secret 内容。当前 `.proflow/runtime/external-resources/dev-tunnel/setup.json` 不存在，证明本轮 install/status/start 没有创建新的 Tunnel ownership 或远端资源。

同时确认一个新的 Git hygiene 问题：根基线 `.gitignore` 没有 `.proflow/`，因此安装后整个包含本地 token 的运行目录显示为 `?? .proflow/`。不得要求普通用户自行识别或处理这些内部敏感运行文件；修复前也不得把该目录加入 Git。

### 15.3 安装后、尚未配置时的 status

真实输出为：

```text
15 配置已完成 · 3 根阻塞 · 5 下游等待 · 0 失败 · 0 个真实进程运行中
PLATFORM_READY=NO
```

状态事实本身正确：三个真实根前置项是 Browser Extension、Dev Tunnel、Model Provider，五个模块等待上游，没有进程运行，也没有假 READY。

但普通用户呈现仍不合格：

1. 用户尚未做任何配置，却看到“15 配置已完成”，容易理解成自己已经完成了 15 项；应改成“基础能力已自动准备”或只显示用户步骤进度；
2. status 展开 23 个内部模块、版本、依赖和运行形态，信息过载；
3. 同时给出三个 package/module setup 命令，让用户在 Browser、Tunnel、Provider 之间自行选择，违背 Platform 唯一入口；
4. 普通模式只应显示核心进度、当前唯一动作和统一 `platform setup`；完整模块诊断应进入显式详细模式；
5. 当前第一步必须固定为 Browser，不能把三个根项平铺成并列选择题。

建议普通输出心智为：

```text
ProFlow 尚未完成配置
进度：0/3

下一步：安装并连接浏览器扩展
运行：platform setup
```

### 15.4 尚未配置时直接 start

真实 `platform start`：

- 正确拒绝启动；
- `PLATFORM_READY=NO`；
- 没有启动 Platform Host、Gateway 或其他进程；
- 没有创建 Tunnel；
- 最终给出统一的 `platform setup --workspace "/Users/agent/Desktop/proton-workspace"`。

因此 start 的 fail-closed 和零部分启动行为为 PASS。

但输出仍重复展开三个根模块、五个下游模块及内部依赖，普通用户不需要这些内容。建议默认只显示：

```text
ProFlow 尚未完成配置，未启动任何服务。
下一步：platform setup
```

如需诊断，再通过明确的详细模式展开内部模块状态。

### 15.5 新增补充问题登记

以下编号追加在原始 P1～P18 之后，不覆盖原登记：

| ID | 真实问题 | 当前判断 | 建议归属 |
|---|---|---|---|
| P19 | 未安装任何模块时 `status` 输出 `PLATFORM_READY=YES` | 确认的假 READY，必须 fail closed | Batch D / Final replay |
| P20 | `steup` 等近似拼写只输出 `INVALID_REQUEST`，没有命令建议 | 确认的 CLI 恢复 UX 缺口 | Batch D |
| P21 | install 后 `.proflow/` 未被根 Git 忽略，包含 owner-only token 的目录显示为 untracked | 确认的 Git/secret hygiene 问题；禁止自动提交 | Batch E / 安全边界 |
| P22 | install 前后各刷 23 个内部模块，输出长且近似重复 | P1 的真实 0.1.40 延续证据 | Batch D |
| P23 | 未配置时 status 写“15 配置已完成” | 状态数字正确、用户语义误导 | Batch D |
| P24 | status 同时展示三个 module-level setup 命令 | 违背 D1/D2 的 Platform 唯一入口与一次一个动作 | Batch A / D |
| P25 | start fail-closed 正确，但失败页重复暴露根模块、下游模块和内部依赖 | 核心行为 PASS，默认呈现仍需收敛 | Batch D |

当前仍然禁止因上述发现直接修改实现。继续人工验收时，下一条真实主流程命令仍为：

```text
platform setup
```

必须按 Browser → Tunnel → Provider 顺序逐步观察；任何真实失败先记录、查清、汇报，再等待整改授权。
