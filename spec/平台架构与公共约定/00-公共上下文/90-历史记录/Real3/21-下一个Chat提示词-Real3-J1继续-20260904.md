# 下一个 Chat 提示词｜Real-3 J1 继续｜0.1.38 Fresh Error Reproduction

你接管 ProFlow Phase 3 Real-3 主线。

仓库：`/Users/agent/Desktop/proton-workspace/repos/proflow`
Workspace：`/Users/agent/Desktop/proton-workspace`

先按顺序读取：

1. `spec/平台架构与公共约定/00-公共上下文/README.md`
2. `spec/平台架构与公共约定/00-公共上下文/01-长期规则/05-执行纪律与工具规则.md`
3. `spec/平台架构与公共约定/00-公共上下文/02-当前接力/CURRENT.md`
4. `spec/平台架构与公共约定/00-公共上下文/03-自动化知识库/基础动作/Browser-UI自动化.md`
5. `spec/平台架构与公共约定/00-公共上下文/03-自动化知识库/包能力/execution-browser-extension.md`
6. `spec/平台架构与公共约定/00-公共上下文/03-自动化知识库/流程/Real3-J0-J4.md`
7. `spec/平台架构与公共约定/00-公共上下文/90-历史记录/Real3/20-Real3当前Chat交接-20260904.md`

读完直接执行，不重复给用户讲长历史。

## 固定门禁

```text
ARCHITECTURE=FROZEN
REAL_1=PASS
REAL_2=PASS/FROZEN
DEPLOYMENT_SUCCESS=YES/FROZEN
CURRENT_EXECUTION_GATE=REAL_3_J0_J4
REAL3_BROWSER_CARRIER_AUTOMATED_TEST_GATE=PASS
REAL_3=NOT_PASS
PHASE3_FINAL_GO=NO
```

## 当前 authority

```text
HEAD = 629e0dbd78e06325831883b24dc6b859ed0edef0
execution-browser-extension Registry/Workspace = 0.1.38
platform-host Registry/Workspace = 0.1.17
Chrome Extension = ProFlow Execution Browser 0.1.38
Extension ID = eehdadpmjffomabiedcjijiakconalab
repo/node_modules/materialized browser artifacts SHA = MATCH
```

不要 publish/update；版本与物化已闭环。

固定资源：

```text
Task = task-a6f859c00b1accd027d53d48
Product = g-6a97182669f88191a943e806a3e1b42a
Dev = g-6a9717f68ef081918aacd5911916d2ec
Test = g-6a97186190108191bc24fb85b2cff584
Product execution = execution:c3fc60b1-74fc-4cfb-bf45-c9ae18602631
Dev execution = execution:a510cb5d-2a65-419f-bd7e-254b9d77cdc4
Test execution = execution:567be187-f585-4d08-bb33-6fbe25e84aed
```

旧 executions 已有 UNKNOWN；禁止第二次盲 Recover，禁止新建 Task/GPT/Execution。

## 你现在只做一个 checkpoint

先证明 Extension errors 页面里的 `Cannot use import statement outside a module` 是 **fresh 0.1.38 error** 还是 **历史 error**。

用户已直接打开 errors detail 并截图；这是主 Chat 上一轮遗漏的必要入口。以后看到 Extension 卡片“错误”，直接进入 errors detail，不要停在卡片层猜根因。

## 第一批动作：禁止先改代码

1. 用 privileged Chrome UI 打开：`chrome://extensions/?errors=eehdadpmjffomabiedcjijiakconalab`。
2. 用 AX tree + screenshot 记录当前错误页；截图就是眼睛。
3. 点击“全部清除”，再次 screenshot 证明 errors 已清空。
4. 启动一次 `platform setup --workspace /Users/agent/Desktop/proton-workspace`，等 CLI 明确进入“正在自动等待扩展连接”。非幂等 Round 记录 PID，结果 UNKNOWN 不盲重跑。
5. pairing server 已监听后，用仓库 canonical `scripts/human-e2e/browser-extension-ui.mjs` reload ProFlow Extension；不要手写临时 AX/Swift helper。
6. 真实 Chrome 回读版本仍为 0.1.38、ID 不变；截图确认。
7. 只 reload 已有 Product GPT 根页一次，触发当前 Content Script fresh injection。用 Playwright snapshot + page screenshot 确认页面 reality；不要创建第二份 Tab。
8. 立即再回 Extension errors detail，用 AX + screenshot 只读取 fresh error。
9. 恢复同一 setup PID 的结果，确认 pairing 成功或 timeout。

## 如何裁决

当前 materialized `dist/extension/content.js` 已证实：

```text
prefix = "use strict"; (() => { ... })
residual ESM import/export = 0
background residual import = 0
```

因此：

```text
fresh import SyntaxError 再出现
→ 当前 runtime 仍存在另一加载路径 / artifact mismatch
→ 精确查 error context/source/path，不要大改

fresh import SyntaxError 不再出现
→ 历史 Content Script error 关闭
→ 禁止继续修改 build pipeline
```

如果 fresh errors 为空但 pairing 仍 timeout，固定沿真实链抓证：

```text
startBackgroundRuntime
→ bootstrapManagedRuntimeConfig
→ runBridgeLoop
→ POST /v1/session/hello
→ first GET /v1/commands/next
→ POST /v1/session/heartbeat
```

不要先猜 Service Worker，不要先改 timeout，不要重新发布。需要源码归因再 CodeGraph → Local Dev；Browser 现实优先。

## 旧 Browser 现场不要碰

Product 根页仍有 Test WORKER_BIND draft；Dev conversation 有 Product WORKER_BIND + 旧 getTask Permission；Test conversation 有 Dev WORKER_BIND + 旧 Permission。它们是旧 one-step-lag 证据。

禁止：

- 点旧 Dev/Test Permission
- 清旧 Conversation
- 新建 GPT / Task / Execution
- 第二次盲 Recover
- 复制新的 Product/Dev/Test 业务 Tab

## Extension runtime READY 后

才执行 `platform start`。先观察 startup Observer Recovery，不要同时手点 Recover。

J1 PASS 只认：三个固定 Role 出现正确 `/g/<gid>/c/<worker>`、正确 WORKER_BIND 真实 user message、routine Permission 零人工、TaskRoleBinding 持久化且 Browser/Owner 一致、零新增 UNKNOWN。

J1 PASS 后继续 J2 Confirm/Start → J3 deterministic WAKE → J4 autonomous Agent Loop。

完成 Real-3 后必须再做一次独立变更审计，不能因为流程跑通直接结案。
