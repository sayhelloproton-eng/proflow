# Real-3 当前 Chat 交接｜2026-09-04

## 1. 目标与阶段

```text
ARCHITECTURE=FROZEN
REAL_1=PASS
REAL_2=PASS/FROZEN
DEPLOYMENT_SUCCESS=YES/FROZEN
CURRENT_EXECUTION_GATE=REAL_3_J0_J4
REAL_3=NOT_PASS
PHASE3_FINAL_GO=NO
```

当前只做 Real-3 J1→J4。Deployment 不重开；发现真实缺陷只做 owning package 最小修复。

## 2. 固定业务资源

```text
Task: task-a6f859c00b1accd027d53d48
Title: Real-3 read-only smoke
研发 + 项目总控: g-6a9717f68ef081918aacd5911916d2ec
运营 + 产品经理: g-6a97182669f88191a943e806a3e1b42a
部署 + 测试验收: g-6a97186190108191bc24fb85b2cff584
```

禁止新建 GPT、Task、Execution identity；三个 Worker create 继续复用 stable refs。
## 3. Stable Execution authority

```text
Product: execution:c3fc60b1-74fc-4cfb-bf45-c9ae18602631
Dev:     execution:a510cb5d-2a65-419f-bd7e-254b9d77cdc4
Test:    execution:567be187-f585-4d08-bb33-6fbe25e84aed
```

最新 durable readback：

```text
Product = FAILED + NOT_APPLIED / attempt 8 / DECISION_UNRESOLVED: INVALID_OUTPUT
Dev     = FAILED + NOT_APPLIED / attempt 7 / CONTENT_SESSION_TIMEOUT
Test    = FAILED + NOT_APPLIED / attempt 6 / CONTENT_SESSION_TIMEOUT
```

三个 `task_role_bindings` 仍全部 `worker_ref=null / conversation_locator=null`。

重要语义：`effectStartedAt=null` 只证明 WORKER_BIND business effect 尚未开始，不能再推出“Browser 没有物理变化”。

## 4. Root Cause A 已关闭：真实 Chrome poll 认证

真实 Chrome 请求抓证：HELLO POST 带 `Origin: chrome-extension://...`；`GET /v1/commands/next` 不带 Origin。旧 Bridge 对所有请求强制 Origin，导致 hello=200、poll=401、handler 不命中。

正式修复已发布为 `execution-browser-extension@0.1.36`。后续 session-bound 请求依靠 Bearer + established extensionInstanceId；Origin 若存在仍必须精确匹配，HELLO 仍强制精确 Extension Origin。
真实 0.1.36 验证：

```text
Registry exact/latest=0.1.36
Workspace/materialized/verification=0.1.36
sessionOnline=true
commandConsumerReady=true
t0=true, t+12s=true, same extensionInstanceId=true
ZERO_COMMAND_LIVENESS_PASS=true
PLATFORM_READY 曾达到 YES
```

因此不要重新调查 Origin、204 framing、MV3 idle、command poll 是否真实工作；该 blocker 已由真实 Chrome 关闭。

## 5. Root Cause B 已锁定：runtime Content Script 构建失效

0.1.36 恢复 Browser transport 后，startup Observer Recovery 自动重驱 Dev/Test。真实 Chrome 创建出多份三个 GPT 的 Role 根页，但没有 `/c/<conversation>`，Execution 报 `CONTENT_SESSION_TIMEOUT`。

发布物直接证实：

```text
materialized dist/extension/content.js 第一行：
import { containsSubmittedFingerprint } from "../src/submitted-message.js";
```

manifest 将它作为普通 `content_scripts[].js` 注入；Content Script 不是 module。相反 `provisioning-content.js` 已被 esbuild bundle 为 classic IIFE。主 runtime Content Script 因未 bundle 无法正常执行 `publish() → PROFLOW_CONTENT_OBSERVATION`，Background 的 `waitForObservation()` 只能超时。
候选修复已落源码：`scripts/build-packages.mjs` 现在也将 `extension/content.ts` 使用 esbuild `--bundle --platform=browser --format=iife` 输出到 `dist/extension/content.js`，并检查不能残留 ESM syntax。`custom-gpt-provisioning-transport.test.ts` 补了 build contract，旧 build RED、新 build GREEN。

真实 build readback：

```text
CONTENT_BYTES=4060
HAS_ESM=false
PREFIX="use strict"; (() => { ... })();
HAS_FINGERPRINT_IMPL=true
```

验证：

```text
targeted contract = 3/3 PASS
execution-browser-extension package gate = 108/108 PASS
typecheck = PASS
publishability = PASS
full validation process exit = 0
```

## 6. 当前版本/运行现场

```text
source package version = 0.1.37
Registry exact 0.1.37 = E404
Registry latest = 0.1.36
Workspace materialized manifest = 0.1.36
verification.moduleVersion = 0.1.36
platform runtime processes = none / STOPPED
active terminal sessions = none
```

因此 0.1.37 **尚未发布、尚未 update、尚未 Chrome reload、尚未 pairing/runtime reality 验收**。下一 Chat 从这里继续，禁止把 package source version 误当已发布版本。
## 7. Browser reality / safety

当前 Chrome 有多份三个 GPT 的 Role 根页，例如：

```text
https://chatgpt.com/g/g-6a97182669f88191a943e806a3e1b42a-...
https://chatgpt.com/g/g-6a9717f68ef081918aacd5911916d2ec-...
https://chatgpt.com/g/g-6a97186190108191bc24fb85b2cff584-...
```

没有发现 `/g/<gid>/c/<cid>` Conversation URL。它们是失败 OPEN 留下的物理准备副作用，不是 Worker binding 成功。

在 0.1.37 真实验收前，应先机械盘点/清理这些失败遗留 Role 根页，避免下一次 observation 与旧 Tab 混淆；不要动其它用户普通 ChatGPT 页面。

Playwright controlled group 是 runtime state，重启/重连后会消失。恢复顺序已经沉淀：原业务 Tab 存在 → 加回 controlled group → 重新握手 → browser_tabs/snapshot 验证。不要因为 MCP 只看到 `connect.html` 就复制业务 Tab。

## 8. 下一步执行顺序

1. 发布前再看一次本轮 Browser/build diff；不要做全仓大改。
2. 唯一 publish `0.1.37`，Registry authority 确认 exact/latest。
3. 单包 update → materialization 0.1.37 → Chrome reload。
4. setup server 先监听，再 Extension action/reload 唤醒；verification 0.1.37。
5. 清理失败遗留 Role 根页后 `platform start`；注意 startup Observer Recovery 会自动驱动 missing workers，先观察，**不要同时手点 Recover**。
6. 若 OPEN 观察成功，应继续到 WORKER_BIND；逐角色收集真实 Conversation URL、user message 与 TaskRoleBinding。
7. Product 若再次 `DECISION_UNRESOLVED: INVALID_OUTPUT`，在 Browser Content 修复现实通过后再单独处理 Model decision output；不要预防性回模型域。
8. J1 PASS 后继续 J2/J3/J4。
## 9. 用户最新要求

- 完成目标后必须主动做一次变更审计，不要等用户提醒；要确认认证边界、Runtime readiness、setup pairing、MV3 loop、Content Script build、Observer Recovery、timeout/no-blind-replay、Playwright Harness 均未被本轮修复带偏。
- 有真实卡点及时讲清楚“卡在哪一层、为什么、下一步怎么判别”，不要只说“不需要用户裁决”。
- 用户现在有 Codex 额度；如再次遇到复杂源码判断，应**生成上下文+审计提示词给用户，让用户自己交给 Codex 执行并把结果反馈回来**。不要直接从 Chat 里启动 Codex CLI，那样吞吐低。
- 调试优先小黄鸭/反向验证：源码正常路径与现实矛盾时，先找缺失运行事实，避免连续堆猜测型修复。

## 10. Dirty worktree

工作树仍有大量 Real-3 与先前 dev-tunnel/spec WIP。绝对不要 reset/clean/revert。当前与本轮最直接相关的 unstaged diff 包括：

```text
packages/execution-browser-extension/src/bridge.ts
packages/execution-browser-extension/src/pairing.ts
packages/execution-browser-extension/extension/background.ts
packages/execution-browser-extension/tests/browser-reality-bridge.test.ts
packages/execution-browser-extension/tests/browser-extension-pairing.test.ts
packages/execution-browser-extension/tests/minimal-user-install-integration.test.ts
packages/execution-browser-extension/tests/custom-gpt-provisioning-transport.test.ts
scripts/build-packages.mjs
scripts/human-e2e/browser-extension-ui.swift
CURRENT.md / Browser-UI自动化.md
```

接手时以 `git status` + 当前磁盘为 authority；不要把 staged/unstaged 混合状态当异常清理。