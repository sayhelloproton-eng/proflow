# Real-3 当前 Chat 交接｜2026-09-04｜Browser Carrier 0.1.38 真实运行前沿

## 1. 当前阶段

```text
ARCHITECTURE=FROZEN
REAL_1=PASS
REAL_2=PASS/FROZEN
DEPLOYMENT_SUCCESS=YES/FROZEN
CURRENT_EXECUTION_GATE=REAL_3_J0_J4
REAL3_BROWSER_CARRIER_AUTOMATED_TEST_GATE=PASS
REAL_3=NOT_PASS
PHASE3_FINAL_GO=NO
```

目标仍是固定 Task 的 J1→J4。Deployment 不重开，不新建 GPT/Task/Execution，不用 Fresh Workspace。

## 2. 当前 Git / Release authority

```text
repo = /Users/agent/Desktop/proton-workspace/repos/proflow
workspace = /Users/agent/Desktop/proton-workspace
branch = main
HEAD = 629e0dbd78e06325831883b24dc6b859ed0edef0
```

Browser Carrier 本轮关键提交：

```text
2e6bbd8 refactor(browser): harden Real-3 carrier reality
412d24a fix(browser): close Real-3 carrier lifecycle gaps
edc918f fix(browser): preserve human deny across carrier recovery
341fef6 chore(release): queue browser carrier hardening
629e0db chore(release): version execution-browser-extension, platform-host
```

Registry / Workspace 已闭环：

```text
execution-browser-extension = 0.1.38
platform-host = 0.1.17
```

Registry exact 存在；Workspace package.json、node_modules 已更新；`.proflow/deployment/browser-extension` 已同步。repo build / node_modules / materialized `background.js`、`content.js` SHA 一致。禁止重复 publish/update。

## 3. Browser Carrier 自动化 Gate 已通过

上一轮从真实 composer one-step-lag 与 ChatGPT Permission 故障出发，完成：controlled composer、Permission semantic detector、Platform Host authoritative policy、AUTO_ALLOW/DEFER/HUMAN_REQUIRED、Carrier Attention Web bridge、MV3 restart rebuild、occurrence stale protection、Human Deny precedence 与 Observer dispatch guard。

主 Chat 独立复验：

```text
execution-browser-extension full = 148/148 PASS
platform-host full = 52/52 PASS
root typecheck / architecture / test-governance = PASS
```

不要因为当前 pairing 问题重新推翻该架构；只有 fresh runtime evidence 指向其中才局部修。

## 4. 固定 Real-3 资源

```text
Task = task-a6f859c00b1accd027d53d48
Product role = g-6a97182669f88191a943e806a3e1b42a
Dev role = g-6a9717f68ef081918aacd5911916d2ec
Test role = g-6a97186190108191bc24fb85b2cff584
Product execution = execution:c3fc60b1-74fc-4cfb-bf45-c9ae18602631
Dev execution = execution:a510cb5d-2a65-419f-bd7e-254b9d77cdc4
Test execution = execution:567be187-f585-4d08-bb33-6fbe25e84aed
```

旧 worker.create 的最新失败语义：Product UNKNOWN attempt 11、Dev UNKNOWN attempt 10、Test UNKNOWN attempt 9；bindings 仍 null。UNKNOWN 禁止第二次盲 Recover。

## 5. 当前真实 Browser 现场

Playwright 受控工作集仍是四个原始页面：Tasks、Product、Dev、Test。不要复制业务 Tab。

旧故障证据已截图并保留：

```text
Product root GPT
  [data-message-author-role=user] = 0
  composer draft = WORKER_BIND ...:@tomflow/proflow-agent-test-ops
Dev conversation
  user message = WORKER_BIND ...:@tomflow/proflow-agent-product
  getTask Permission = 始终允许 / 拒绝 / 允许一次
Test conversation
  user message = WORKER_BIND ...:@tomflow/proflow-agent-controller-dev
  getTask Permission = 始终允许 / 拒绝 / 允许一次
```

不要手点这两个旧 Permission。它们属于 0.1.38 之前的一拍错位现场，不是新版本 Permission 自动处理失败证据。

## 6. 0.1.38 pairing 现场

真实 Chrome Extension Manager 已确认：

```text
ProFlow Execution Browser 0.1.38
Extension ID = eehdadpmjffomabiedcjijiakconalab
Service Worker entry 可见
卡片显示“错误”
```

真实 `platform setup` 在 pairing server 等待后 reload Extension，最终 `PAIRING_TIMEOUT`。因此当前 0.1.38 pairing 尚未 READY，不能进入 Worker Recover/J1 重放。

## 7. 用户直接打开 Extension errors detail 后的新证据

错误详情 URL：
`chrome://extensions/?errors=eehdadpmjffomabiedcjijiakconalab`

用户截图显示顶部错误：

```text
Uncaught SyntaxError: Cannot use import statement outside a module
context = https://chatgpt.com/g/g-6a9717f68ef081918aacd5911916d2ec-...
stack = dist/extension/content.js:1
```

下面还有重复 SyntaxError、`Cannot read properties of undefined (reading 'sendMessage')`、`Failed to fetch`。

但必须注意：Chrome Extension errors 页面会保留历史错误。截图里的 source preview 当前已经以 `"use strict"; (() => { ... })` 开头；本机再次机械读取当前 0.1.38 materialized `content.js` 也是 IIFE，扫描 residual imports=0，background residual imports=0。

因此当前准确裁决不是“0.1.38 仍有 import SyntaxError”，而是：**历史 SyntaxError 已证实存在；0.1.38 是否 fresh reproduce 尚未验证。** 不得继续猜 Service Worker 或再次修改 bundle。

## 8. 本 Chat 执行失误与永久经验

看到 `chrome://extensions` 卡片“错误”后，主 Chat 一度只停在卡片层并推断“Service Worker startup/runtime error”，没有第一时间进入 errors detail。用户指出后纠正。

以后固定：Extension 卡片出现“错误” → 直接进入 `chrome://extensions/?errors=<extensionId>` → screenshot/AX 读取具体错误；同时记住 errors 是历史列表，必须清空后 fresh reproduce 才能归因当前版本。

**截图就是眼睛。** privileged Chrome UI 用 AX + screenshot；普通 ChatGPT/Tasks 用 Playwright snapshot + page screenshot。CLI/API success 不替代用户现实。

## 9. 下一 Chat 第一轮动作

严格按以下顺序，不先改代码：

1. 读取 CURRENT、本交接、Browser-UI 与 execution-browser-extension Runbook。
2. 用 privileged Chrome automation 进入当前 Extension errors detail，先 screenshot + AX 记录现状。
3. 点击“全部清除”，截图确认历史 errors=0。
4. 启动一次新的 `platform setup`，等 CLI 明确进入“正在自动等待扩展连接”，记录 PID/round。
5. pairing server 已监听后，执行 canonical Extension reload；确认真实 Chrome 仍显示 0.1.38 / 同一 ID。
6. 只 reload 现有 Product GPT 根页一次以触发 fresh content script injection；立即用 Playwright page screenshot + snapshot 看页面 reality。
7. 回 errors detail，用 AX + screenshot 只收集 fresh errors。
8. 恢复 setup 进程结果：若 paired/READY，关闭当前 blocker；若 timeout，先依据 fresh error/网络现实逐层定位，不盲 rerun。

若 fresh errors 为空但 pairing 仍失败，下一调查链固定为：`startBackgroundRuntime → bootstrapManagedRuntimeConfig → runBridgeLoop → /v1/session/hello → first /commands/next poll → heartbeat`，逐层抓请求/错误，不从猜测开始。

## 10. Real-3 恢复条件

只有 pairing/runtime READY 后才 `platform start`。start 后先看 startup Observer Recovery，禁止同步点 `Recover missing Workers`。旧 UNKNOWN 仍要求 no-blind-replay。

J1 PASS 必须同时有：

```text
3 个正确 /g/<role>/c/<worker> Conversation
3 条正确角色 WORKER_BIND 真实 user message
routine getTask Permission 零人工处理
TaskRoleBinding workerRef + conversationLocator 持久化
Browser reality == Owner authority
WRONG_CONVERSATION=0
WRONG_BIND_MESSAGE=0
UNKNOWN_SIDE_EFFECT=0
```

通过 J1 后才继续 J2 Confirm/Start → J3 deterministic WAKE → J4 autonomous Agent Loop。
