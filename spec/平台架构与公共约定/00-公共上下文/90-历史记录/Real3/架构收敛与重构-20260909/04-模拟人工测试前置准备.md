# 模拟人工测试前置准备

> Owner：真实验收之前的环境事实与 blocker。
> 不复制部署 SOP；正式命令唯一看 `03-工作区重新部署手册.md`。

## 1. 已完成机械前置（2026-09-09）

| 检查 | 当前事实 |
|---|---|
| Repo Node | `v24.19.0` |
| pnpm | `11.21.0` |
| global `platform` | `@tomflow/proflow-platform-cli@0.1.51`，路径 `/Users/agent/.nvm/versions/node/v24.19.0/bin/platform` |
| Chrome | `/Applications/Google Chrome.app` 存在 |
| Browser harness | compile PASS；cold compile `104.011s`，后续应复用缓存 |
| 临时 harness Workspace | `/Users/agent/Desktop/proflow-human-e2e-workspace` 已建立，仅作安全 dry-run/工具演练 |
| Fresh safety dry-run | PASS；临时 Workspace 清理边界正常 |
| 当前 Product Browser setup | READY |
| 当前 Product Model/FAST/THINK setup | READY |
| 当前 Product Dev Tunnel | **FAILED：Tunnel 运行状态检查失败** |
| 当前 Product `PLATFORM_READY` | `NO`（当前 status 原因为远程连接失败） |

## 2. 当前外部 blocker

Dev Tunnel 在正式 candidate 部署/真实 Action 验收前必须恢复。恢复动作只在 candidate ready 后按 Module owner 路径执行；不得现在为“看一眼”反复 setup/start。

如需要 OAuth/人工登录，只执行 owner 给出的真实 Human Action；不删除/重建 Tunnel 试运气。恢复后记录 tunnel identity/public URL/port/observedAt。

## 3. 正式开跑条件

全部满足才允许进入 `03`：

- `02` Step 1～9 Behavior/throughput Gate PASS；
- 三 Provider 真实 transport + smoke PASS；
- 三 Role 新 Action schema conformance + provisioning PASS；
- frozen candidate 已发布并 Registry exact readback；
- source SHA/lock hash 未漂移；
- Browser harness 可用；
- 用户已决定本轮真实 Workspace 是“保留 state 重新部署”还是“Fresh 从零部署”。

## 4. 正式验收必须观察的证据

- Workspace 实际安装 package versions/integrity；
- GPT 实际加载的新 Action schema/version；
- Gateway Role auth；
- Host tool audit：role/tool/operation/duration/outcome；
- simple Tool path 的 Task query=0、Execution call=0；
- Repomix/Local Dev/CodeGraph 各至少一次真实结果；
- Product/Dev/Test 的 TaskDocument 与 Node transitions；
- Dev complete 后 Test 的系统自主 WAKE；
- 最终真实 Task terminal；
- Browser/Tunnel/Provider/runtime 的 observedAt。

## 5. 当前不要做

- candidate 未冻结前不在真实 Product Workspace 做最终 install/setup/start。
- 不拿临时 Workspace PASS 冒充正式部署。
- 不拿 fake Provider/内部注入冒充真实 Tool Action。
- 不为了 Tunnel/Browser 状态反复 mutation。
- 不在 Final Gate 现场修源码。
