# 下一个 Chat 提示词｜Real-3 J1 继续｜2026-09-04

你接管 ProFlow Phase 3 Real-3 主线。

仓库：
`/Users/agent/Desktop/proton-workspace/repos/proflow`

Workspace：
`/Users/agent/Desktop/proton-workspace`

先按顺序读取：

1. `spec/平台架构与公共约定/00-公共上下文/README.md`
2. `spec/平台架构与公共约定/00-公共上下文/01-长期规则/05-执行纪律与工具规则.md`
3. `spec/平台架构与公共约定/00-公共上下文/02-当前接力/CURRENT.md`
4. `spec/平台架构与公共约定/00-公共上下文/03-自动化知识库/流程/Real3-J0-J4.md`
5. `spec/平台架构与公共约定/00-公共上下文/90-历史记录/Real3/18-Real3当前Chat交接-20260904.md`

读完直接执行，不重复向用户复述历史，不重开 Deployment。

## 固定门禁

```text
ARCHITECTURE=FROZEN
REAL_1=PASS
REAL_2=PASS/FROZEN
DEPLOYMENT_SUCCESS=YES/FROZEN
CURRENT_EXECUTION_GATE=REAL_3_J0_J4
REAL_3=NOT_PASS
```
## 固定资源，绝对不要重建

```text
Task = task-a6f859c00b1accd027d53d48
研发 + 项目总控 = g-6a9717f68ef081918aacd5911916d2ec
运营 + 产品经理 = g-6a97182669f88191a943e806a3e1b42a
部署 + 测试验收 = g-6a97186190108191bc24fb85b2cff584
Product execution = execution:c3fc60b1-74fc-4cfb-bf45-c9ae18602631
Dev execution = execution:a510cb5d-2a65-419f-bd7e-254b9d77cdc4
Test execution = execution:567be187-f585-4d08-bb33-6fbe25e84aed
```

禁止创建第 4/5/6 个 GPT；禁止新建 Real-3 Task；禁止手工制造新的 Execution identity。

## 当前准确 checkpoint

Browser poll Origin 根因已经在 `0.1.36` 真实关闭，不再重查。真实 zero-command liveness 已 PASS。

当前 blocker 是 runtime Content Script 构建产物：0.1.36 materialized `dist/extension/content.js` 仍含顶层 ESM import，但 manifest content script 是 classic script，导致 `PROFLOW_CONTENT_OBSERVATION` 不发布，OPEN 最终 `CONTENT_SESSION_TIMEOUT`。

候选修复已经落盘并通过完整 Gate：`scripts/build-packages.mjs` 将 `extension/content.ts` 也 bundle 为 classic IIFE，并做无 ESM hard guard。

```text
source version = 0.1.37
Registry exact 0.1.37 = E404
Registry latest = 0.1.36
Workspace/materialized/verification = 0.1.36
platform = STOPPED
package gate = 108/108 PASS
typecheck = PASS
publishability = PASS
```

所以你的第一件事不是继续排查源码，而是把这个已验证 candidate 做真实发布/安装闭环。
## 第一批执行动作

1. 只读确认 `git status`、Browser Extension candidate diff、Registry 0.1.37 空位；不要改无关 dirty WIP。
2. 唯一 publish `@tomflow/proflow-execution-browser-extension@0.1.37`。publish 返回不明确时，只查 Registry authority，禁止盲重发。
3. `platform update --package @tomflow/proflow-execution-browser-extension --workspace /Users/agent/Desktop/proton-workspace`；确认 materialized manifest=0.1.37。
4. Chrome privileged reload；setup/pairing 时序必须是：setup server 先监听 → 再 reload/action 唤醒 Extension → verification readback=0.1.37。
5. 启动平台前先盘点当前 Chrome 中失败 OPEN 留下的重复 Role 根页。它们不是 Conversation；只清理这批确定的失败 Role 根页，不动用户其它 ChatGPT Tabs。
6. `platform start` 后先看 startup Observer Recovery；它会自动 `ensureWorkers`，所以**不要同时手点 Recover**。
7. 立即收集 Browser + Owner readback：Content observation 是否出现、OPEN 是否成功、是否进入 WORKER_BIND。

## 当前 Browser residue

上一轮 Observer 自动 Recovery 已真实创建多份三个 GPT 的 `/g/g-...` Role 根页，但未发现任何 `/c/<conversation>` URL。Dev/Test 因 Content Script 未运行而 `CONTENT_SESSION_TIMEOUT`。

所以：

```text
effectStartedAt=null
= WORKER_BIND business effect 未开始
!= Browser 没打开过 Tab
```

禁止因 `NOT_APPLIED` 直接盲重试。先 reconcile/清理失败 OPEN 物理残留。

## J1 PASS 标准

三个固定 GPT 都必须看到：

```text
真实 /g/<gid>/c/<cid> Conversation URL
真实 WORKER_BIND user message
TaskRoleBinding.workerRef != null
TaskRoleBinding.conversationLocator != null
Browser reality 与 Owner readback 一致
```

不能用内部 DB/API 单独宣称 PASS，也不能用 UI 单独宣称 PASS。
## 工具与执行纪律

- 仓库理解先 Repomix（sandbox root 是 `/Users/agent/Desktop/proton-workspace/repos`，参数必须相对，例如 `proflow/packages/execution-browser-extension`），再 CodeGraph 看结构/blast radius，再 Local Dev 读当前磁盘/改/测，最后 Playwright 看真实 Browser。
- Playwright controlled group 不是持久态；重启后先恢复原 Tabs 到 group，再重新握手。不要复制开业务 Tab。
- 普通网页自动化不得随意 activate Chrome；只有 Chrome privileged UI 才可短暂激活并恢复原前台应用。
- 非幂等 publish/update/setup/recover 结果不明确时，先查 authority，禁止盲重试。
- dirty worktree 必须保留，不 reset/clean/revert。

## 若再次卡住

先讲清楚当前具体层级和证据，不要用“还在处理”“不需要裁决”代替问题说明。优先小黄鸭：重新从源码讲完整链路，再拿现实逐层对照。

用户现在有 Codex 额度；如需要独立代码审计，**不要直接运行 Codex CLI**。生成一份完整上下文+审计提示词给用户，由用户交给 Codex，用户会把结果贴回来，再做交叉裁决。

## 完成后必须自审计

Real-3 目标真正跑通后，不要直接结案。主动审计本轮修改是否引入其它问题，至少覆盖：

```text
Origin/session auth 是否仍 fail-closed
sessionOnline 与 commandConsumerReady 是否职责清楚
setup pairing 与 Runtime Bridge 是否都兼容真实 Chrome
5s bridgeFetch watchdog 是否只属于 loopback transport
20s keepalive / 120s watchdog 是否仍有不合理副作用
Content Script 与 provisioning content 都是自包含可执行产物
Observer startup recovery 是否会制造重复 OPEN/盲重放
OPEN physical side effect 与 WORKER_BIND durable boundary 是否仍安全
Playwright Harness 是否没有污染产品路径
完整 package tests/typecheck/build/publishability + 真实 J1-J4 evidence
```

最终只有“真实流程通过 + Owner readback 一致 + 变更审计无新增阻塞问题”才算完成。