# 下一个 Chat 提示词｜Real-3 J0→J4 继续｜2026-09-03

```text
你现在接管 ProFlow Phase 3 Real-3 主线。

仓库：
/Users/agent/Desktop/proton-workspace/repos/proflow

Product Workspace：
/Users/agent/Desktop/proton-workspace

当前最高权威状态：
ARCHITECTURE = FROZEN
REAL_1 = PASS
REAL_2 = PASS / FROZEN
DEPLOYMENT_SUCCESS = YES / FROZEN
CURRENT_EXECUTION_GATE = REAL_3_J0_J4
PHASE3_FINAL_GO = NO

你的唯一目标：
继续真实 Real-3 J0→J4，直到完整真实 evidence 足以裁决 Real-3。不要重开 Deployment，不要把工具吞吐优化单独变成主线。

第一步只按顺序读取：
1. spec/平台架构与公共约定/00-公共上下文/README.md
2. spec/平台架构与公共约定/00-公共上下文/01-长期规则/05-执行纪律与工具规则.md
3. spec/平台架构与公共约定/00-公共上下文/02-当前接力/CURRENT.md
4. spec/平台架构与公共约定/00-公共上下文/90-历史记录/Real3/16-Real3当前Chat交接-20260903.md
5. spec/平台架构与公共约定/00-公共上下文/03-自动化知识库/流程/Real3-J0-J4.md
```
```text
接管后先机械确认易漂移真值：
- git branch / HEAD / working tree
- Product Workspace 的 platform status
- 不要把交接里的 HEAD/runtime 快照当成永恒真值

当前最后机械快照：
branch = main
HEAD = c85e986b56eca8be3e5c016a14bc1470ee656d87
source dev-tunnel = 0.1.32
installed dev-tunnel = 0.1.32

最新 platform status：
3/3 核心配置全部完成
Browser = READY
Remote = READY
Model + FAST/THINK = READY
PLATFORM_READY=NO
reason = ROLE_NOT_REGISTERED:@tomflow/proflow-agent-controller-dev

同场景 platform setup 最后出现：
agent-controller-dev
SETUP_FAILED — PROVISIONING_EXTENSION_OFFLINE
HUMAN_SETUP_RC=1

因此当前唯一真实主线阻断 = Real-3 J0 的 Role 注册 / Provisioning Extension 链。
不要重新调查 Dev Tunnel、Browser、Model，也不要 Fresh/install。
```
```text
工具纪律必须遵守用户最新裁决：
1. 仓库理解/修改默认 Repomix first。
2. 窄域也要先 pack 当前 package/最小目录；不要因为 symbol 已知就跳过 Repomix。
3. 当前第一包从 packages/agent-controller-dev 开始。
4. 一次批量读清实现/tests/deployment/manifest；只有证据要求时才向 provisioning/role 邻接 owner 扩一层。
5. 再用 CodeGraph 证明 caller/callee、ownership、blast radius。
6. Local Dev 做修改、测试、Git、发布；Browser authority 用 Playwright。
7. Repomix 吞吐改进只在真实 Journey 中自然验证，不单独开专项。

Real-3 路线必须保持：
J0 3 Role/GPT READY
→ J1 New Task / PENDING / Worker / Conversation Binding
→ J2 confirm / startTask / prerequisites / first Node READY / ACTIVE
→ J3 READY Node / Observer / Restore / WAKE / ChatGPT real submit
→ J4 Worker autonomous turn / Reason / Action / Owner API / Result

语义红线：
Role != Worker
WAKE success != Node success
Browser submit API success != 用户消息已出现
ChatGPT submit 最终必须由真实 DOM [data-message-author-role="user"] 证明

如果 J0 证明确有 owning-package defect：
Repomix → CodeGraph → 最小改动 → targeted regression/typecheck/build as needed
→ 按 Package-Update-Loop 真实发布 → Registry exact → platform update --package
→ 立即返回 J0 Journey checkpoint
```
```text
禁止事项：
- 不 Fresh Workspace，不重新 platform install，不重开 Deployment。
- 不重新折腾 Browser Extension / Dev Tunnel port-list / 模型域；当前三项 core status 都 READY。
- 不安装或复用 @tomflow/proflow-dev-tunnel@0.1.31；该 Registry 版本含 workspace:^，是坏发布物。正确现场版本 = 0.1.32。
- 禁止裸 npm publish。workspace package 必须走仓库 pnpm workspace release/publish 路径，并在发布前后 Registry exact readback。
- 不 reset/clean/revert 现有 dirty worktree 的无关修改。
- 不创建私有 gptweb-mcp manager，不用旧 CDP 9229，不预防性重启 Chrome。
- 不 git push，除非用户明确授权。
- 不把 test/typecheck/build 绿灯当成 REAL_3 PASS。

当前源码还存在未提交的 dev-tunnel、human-e2e、公共上下文等变更；接管时以 git status 和实际文件为权威，不要覆盖别人工作。

你不需要重新向用户复述 Deployment 历史。读完上下文后直接从 J0 继续执行；只有出现新的真实可复现缺陷时才插入最小 repair loop，修完立刻回 Journey。
```
