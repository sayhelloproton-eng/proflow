# Active Context 重复归类审计｜2026-09-08

## 目标

本轮不删除知识，只消除 active context 中的多 owner。原始长版已逐字归档。

## 归类裁决

1. `01/05` 只拥有不可越界执行铁律；完整四 Plane/Batch/Reality/Hotfix SOP 归 `Chat-高吞吐本地工程执行.md`。
2. `03/00` 降级为跨项目方法总览，不再复制 SAME-SCENE、UI、PID、Gate 的具体 SOP。
3. `Tool-Runtime-gptweb-mcp.md` 只拥有 runtime/manager/relay/token/sandbox/连接恢复；工具任务调度归 `Chat-高吞吐本地工程执行.md`。
4. `Real3-J0-J4.md` 只拥有 Journey/checkpoint；Browser mechanical knowledge 归 Browser/Tool-Runtime/Extension owner。
5. `03/README` 增加唯一知识 Owner 矩阵与判重规则。

## 保留的有意镜像

`Browser/UI reality-first`、`UNKNOWN 禁止盲重试`、`CURRENT 漂移先写回` 等缺失一句就可能改变执行路径的护栏允许在 README/长期规则/CURRENT 出现短句镜像；完整 SOP 只能在唯一 owner。

## 无损性

归类前五份 active 文档已保存在本目录 `20260908-归类前-*` 快照；历史证据不参与 active authority，但可按需追溯。

## 结果指标

- active Markdown：28 份，文件数未通过删除知识降低。
- active 总行数：`2880 → 2593`，减少 287 行重复展开；CURRENT 本轮逐字未改。
- `03/00`：`201 → 61`，降级为方法总览。
- `01/05`：`197 → 84`，只保留执行铁律。
- `Tool-Runtime`：`185 → 140`，移除任务调度重复，只保留 runtime/sandbox/relay/token。
- `Real3-J0-J4`：`138 → 132`，移除通用 Browser SOP 重复。
- `03/README`：`66 → 83`，增加唯一知识 Owner 矩阵与判重规则。
- exact repeated paragraph group：0。
- CURRENT 与治理前 commit `0baab57` 逐字一致；REQUIRED_CONTEXT 三条路径全部存在。
- 五份归类前 active 原文与治理前 Git 内容 SHA/bytes 逐字一致，全部已归档。

## 历史目录归类

纯上下文治理的旧快照、审计、瘦身记录统一迁入 `90-历史记录/上下文治理/`；`90-历史记录` 根只保留总时间线及 `Deployment / Real3 / 上下文治理` 分类入口。迁移前已确认没有 active 文档引用旧路径。
