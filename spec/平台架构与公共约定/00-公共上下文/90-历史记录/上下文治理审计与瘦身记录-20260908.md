# 公共上下文治理审计与瘦身记录｜2026-09-08

> 性质：一次性治理记录。当前执行真源仍只有 `02-当前接力/CURRENT.md`。

## 审计结论

本轮主要问题不在长期规则，而在当前控制面发生历史堆积：治理前 `CURRENT.md` 已增长到约 1039 行，同时包含 Round 2/3/4、Chrome adoption、多个已消费 acceptance 与早期 0.1.49 reality。新 Chat 即使遵守最小读取顺序，也会同时吸收互相矛盾的旧/新 authority，例如旧 `verification=0.1.49` 与当前 `verification=0.1.50` 同处 `CURRENT_AUTHORITY`。

第二个结构问题是 `02-当前接力/` 仍保留 2026-09-05 的 J3 接手提示词，与“CURRENT 是唯一当前入口”的治理规则冲突。

第三个知识迁移缺口发生在 Dev Tunnel：历史经验已经知道 login/remote query 会随时间失效，但“本地 phase=READY 不代表 remote auth/tunnel/port reality”没有被写成高成本 production start 前的机械门禁，导致 2026-09-08 唯一一次 start 被 expired login 消费。

## 本轮处理

1. 治理前 CURRENT 完整归档为 `90-历史记录/Real3/27-Real3-J4-0.1.50采用到ProductionStart-DevTunnel失败-CURRENT快照-20260908.md`。
2. CURRENT 重写为纯当前控制面，只保留：阶段、实时 authority、固定 SAME-SCENE、当前 blocker、已证明 Dev Tunnel 现场、known defect、tool route、NEXT_ACTION、当前 mutation authority、DO_NOT_REPEAT、REQUIRED/ON_DEMAND context、STOP_POINT。
3. 旧 J3 接手提示词从 `02-当前接力/` 迁到 `90-历史记录/Real3/`。
4. `dev-tunnel.md` 新增 production start 前 remote reality gate：login → tunnel → exact port；明确本地 READY 不能替代远端 authority。
5. `dev-tunnel.md` 登记当前诊断语义缺口：具体 `Login token expired` 被压成 UNKNOWN / TUNNEL_RUNTIME_FAILED，等待 owning package 修复。
6. README 明确 `02-当前接力/` 正常只保留 CURRENT。

## 保留原则

没有删除 Round evidence、hash、acceptance 或 Chrome harness 细节；它们从默认当前注意力迁到了历史快照。长期执行规则、Frozen architecture、正式 Spec 均未修改。

## 失忆新 Chat 验收标准

不给任何聊天历史，只读取：

```text
README
→ 01/01 总控
→ 01/02 治理
→ 01/05 执行纪律
→ CURRENT
→ CURRENT.REQUIRED_CONTEXT
```

应直接得出：当前不是 Browser blocker；0.1.50 Chrome adoption 已 PASS；唯一 production start 已失败并消费；当前第一 authority 是 Dev Tunnel remote auth；已证明 token expired；remote tunnel/41705 port existence 仍 UNKNOWN；下一步先 canonical login recovery + remote readback，再修诊断缺陷，之后才允许重新冻结 production start。若模型仍会重跑 Chrome Round、直接第二次 start、或根据本地 READY 推导 remote READY，则治理失败。
