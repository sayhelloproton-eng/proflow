# ProFlow Phase 3｜Deployment Closeout 当前唯一上下文与长期执行交接

> 更新时间：2026-08-30。
> 文件名保留 `09-Real3...` 仅为避免破坏既有引用；**当前执行阶段不是 Real-3 Journey，而是 Deployment Closeout。**
> 本文替代原公共上下文 `09～17` 的过程性交接文件，作为当前唯一滚动执行入口，也是下一 Chat 长期连续执行的主上下文。
> 原 `09～17` 中已经 PASS、废弃、重复或仅有历史追溯价值的内容不再恢复；需要历史原因时查 Git history 与正式 spec/evidence。
> 易漂移事实（HEAD、working tree、Registry、runtime、远端 Tunnel、MCP runtime）接管时必须机械重读，不能把本文快照当实时真值。


## 0. 2026-08-30 本 Chat 最新收口（下一 Chat 先读本节）

> 收口时间：2026-08-30 23:40 左右。
> 本节是当前 Chat 的最新执行快照，**覆盖本文后面仍保留的旧 HEAD、旧版本、旧 Gate 描述**；后文继续保留 Root、方法和历史原因。
> 下一 Chat 接管后仍必须机械重读 Git / Registry / Product Workspace / Chrome / runtime，不能仅凭本节直接宣判 PASS。

### 0.1 当前机械真值

```text
Source Repo       = /Users/agent/Desktop/proton-workspace/repos/proflow
Source Branch     = main
Source HEAD       = b874a42
Source WorkingTree= CLEAN
Product Workspace = /Users/agent/Desktop/proton-workspace
DEPLOYMENT_SUCCESS= NO
```

当前产品工作区已安装的关键 Registry 版本：

```text
@tomflow/proflow-platform-cli                 0.1.43
@tomflow/proflow-agent-runtime                0.1.11
@tomflow/proflow-execution-browser-extension  0.1.18
@tomflow/proflow-dev-tunnel                   0.1.20
@tomflow/proflow-model-provider-api           0.1.17
@tomflow/proflow-model-runtime                0.1.20
@tomflow/proflow-agent-gateway                0.1.15
@tomflow/proflow-agent-controller-dev         0.1.15
@tomflow/proflow-agent-product                0.1.15
@tomflow/proflow-agent-test-ops               0.1.15
```

npm Registry 已机械确认存在：

```text
@tomflow/proflow-agent-runtime@0.1.11
@tomflow/proflow-platform-cli@0.1.43
```

并已重新 `npm pack` 这两个 exact version 进行 tarball readback，确认：

```text
agent-runtime 0.1.11 tarball
→ dist/deployment/adapter.js 存在
→ 包含 role-credentials.json owner-store 初始化逻辑
→ CHANGELOG 包含 Fresh deployment deadlock 修复说明

platform-cli 0.1.43 tarball
→ dist/src/lifecycle/thin.js 存在
→ 包含 temporaryRuntimeRefs / temporaryRuntimeSet
→ agent-package setup 前临时 bootstrap dependency closure
→ setup 收尾逆序清理本轮临时启动 runtime
```

因此：

```text
REGISTRY_AGENT_RUNTIME_0_1_11_EXACT = PASS
REGISTRY_PLATFORM_CLI_0_1_43_EXACT  = PASS
```

**禁止下一 Chat 再次 publish 这两个相同版本。** 若任何发布动作出现 UNKNOWN，只允许先 `npm view/npm pack` exact readback，禁止盲目重发。

当前产品 Workspace 已再次处于 Fresh-ish、fail-closed 基线：

```text
platform status
→ 已读取 23 个模块
→ 配置进度 0/3
→ 1/3 Browser = 当前步骤
→ 2/3 Remote  = 后续步骤
→ 3/3 Model   = 后续步骤
→ 0 个真实服务进程运行中
→ PLATFORM_READY=NO
```

当前 Agent durable state：

```text
.proflow/agent/roles.json                  = MISSING
.proflow/agent/secrets/role-credentials.json = PRESENT, 0 keys
```

当前 `.proflow` 仍存在若干 owner/shared-facts 文件与 task SQLite；**不得把文件存在当 READY**，当前 public `platform status=0/3` 才是产品 fail-closed 真值。最终 Fresh replay 是否需要再次 controlled clean，由下一 Chat 先机械核验安装来源/文件时间/Chrome registration 后裁决；不能从旧 evidence 继承 PASS。

### 0.2 本 Chat 从 Model 到 Agent Deployment 的完整推进

#### A. Model Runtime：MR-01 / MR-02 / MR-03

本 Chat 前半段先完成 Model Runtime 三个真实根因整改：

```text
MR-01 deployment capability probe 使用 1×1 PNG
→ 真实 Qwen Vision API 因图片尺寸约束 HTTP 500
→ 导致 VLM 假阴性
→ 改为合法 64×64 Vision fixture

MR-02 runtime start 路径还藏着第二份 1×1 PNG
→ setup READY，但 start 再次 HTTP 500
→ setup/start 改为共享同一个 64×64 fixture

MR-03 setup 与 start 对 structured output 的证明方式不一致
→ setup 用 native response_format 的结果误标 prompted
→ start 真按 prompted 调用后收到 reasoning prose，JSON.parse 失败
→ 改为 native → 真 prompted fallback
→ setup/start/runtime 共用 fail-closed structured/thinking 归一化
→ 支持 reasoning_content / <think> 证据
→ prompted 只允许裸 JSON 或“整个响应只有一层 JSON fence”，禁止从任意 prose 中抠 JSON
```

Model Runtime 验证：

```text
model-runtime tests = 65/65 PASS
model-runtime typecheck = PASS
git diff --check = PASS
SOURCE_REAL setup → start → status → stop = PASS
```

发布结果：

```text
@tomflow/proflow-model-runtime 0.1.19 → 0.1.20
Registry exact readback = PASS
```

关键提交：

```text
20f0483  fix: align model runtime capability verification
abe2043  docs: tighten deployment closeout execution discipline
a8d69e9  chore: version model runtime capability verification
```

#### B. Fresh Registry replay：Browser / Tunnel / Model

随后重新受控清理真实产品 Workspace，从 Registry 做 Fresh replay：

```text
23/23 active modules install = PASS
Browser 0.1.18 Fresh remove-old-registration → load-unpacked → enabled → Service Worker → pairing/heartbeat = PASS
Dev Tunnel 0.1.20 Fresh setup / host / READY = PASS
Model Provider 0.1.17 + Runtime 0.1.20 Fresh setup = PASS
FAST  = sayhelloproton/Qwen3.5-4B-MLX-4bit-no-think
THINK = mlx-community/Qwen3.5-4B-MLX-4bit
Model setup failure artifact = ABSENT
当时 platform status 已达到核心配置 3/3，但仍未满足完整 Deployment，因为 Agent 创建与正式 runtime 尚未闭环
```

Browser Fresh 的关键纠偏：必须真实 Remove 旧 Chrome registration，再从当前 Fresh 安装目录 Load unpacked；Reload 不能冒充 Fresh install。macOS AX 原生 `AXPress` 最终稳定完成 Remove / confirm / load；Playwright 负责真实页面观察，但 `chrome://extensions` 原生 UI 不强行走 DOM。

#### C. 本 Chat 一次包管理器污染事故及恢复

Fresh Workspace 实际是 npm-owned。执行者曾错误使用：

```text
pnpm exec platform status
```

结果 pnpm 在 Platform 代码运行前把 23 个 ProFlow 包移动到：

```text
node_modules/.ignored
```

并生成 `pnpm-lock.yaml`，造成混合 package-manager 污染。恢复方式：

```text
确认 package-lock.json 精确锁住完整 23 包
→ 删除污染 node_modules
→ npm ci
→ .ignored = 0
→ 后续产品入口只使用 ./node_modules/.bin/platform
```

**下一 Chat 禁止在 npm-owned 真实产品 Workspace 使用 `pnpm exec platform ...`。** Repo 工程命令可以继续按仓库 `pnpm` 规范；产品 Workspace 与 source repo 的 package-manager 语境必须分开。

#### D. 用户裁决：智能体真实创建是 Deployment 必要 Gate

用户明确纠正：

```text
Real-2 历史 Custom GPT PASS
≠
当前 Deployment 可以跳过智能体创建
```

最终规则已落 `05/09/10`：

```text
Custom GPT / 智能体真实创建 = DEPLOYMENT_SUCCESS 必要 Gate
Real-2 历史 PASS = 仅作能力回归参考
当前 Fresh Deployment 必须重新真实创建并核对 ChatGPT + identity/config/evidence
```

#### E. Fresh Deployment 新暴露的 Agent/Gateway lifecycle deadlock

在 Browser/Tunnel/Model 均已 READY 后，三个 Agent 仍是：

```text
ACTION_REQUIRED / ROLE_NOT_REGISTERED
```

公开 `platform start` 真实返回 RC=1，且：

```text
execution-runtime STOPPED
model-runtime     STOPPED
platform-host     STOPPED
agent-gateway     STOPPED
```

根因链：

```text
Agent setup
→ 真实创建 GPT 后还必须通过 Gateway /health + Bearer Action probe

Gateway HTTP runtime
→ 只有 start 后才真正运行

platform start
→ 又要求所有 Agent setupStatus 先 READY

形成死锁：
Agent READY 要 Gateway RUNNING
Gateway RUNNING 要 platform start
platform start 又要求 Agent READY
```

同时暴露第二层 owner truth 问题：

```text
Fresh .proflow/agent/secrets/role-credentials.json = ABSENT
agent-gateway start 明确要求它存在
但 agent-runtime deployment adapter 当时只是空壳：install/setup 永远 SUCCEEDED，status 永远 READY/NOT_APPLICABLE
```

真实 `createAgentRuntime()` 本身会原子初始化空 role credential store，所以整改没有让 Platform Host/Gateway 越权代建，而是让 Agent Runtime owner 自己兑现 Deployment truth。

#### F. Agent Runtime + Platform CLI 最小整改

Agent Runtime：

```text
install/setup
→ createAgentRuntime(.proflow)
→ 原子创建 owner durable role credential store / collaboration storage
→ close()

status
→ 真实观察 owner store，而不是 fake READY

Descriptor/proflow.module.json
→ 声明 filesystem / preserve durable owner-state effect
```

Platform CLI：

```text
setupModulesThin
→ 在 agent-package provisioning 前计算 dependency closure
→ 只临时 start 当前 STOPPED 的 service prerequisites
→ 已 RUNNING 服务不重复 start、不记入 cleanup
→ Agent setup 成功/失败都在 finally 中逆依赖顺序 stop 本轮临时启动服务
→ 保持用户正式顺序 install → setup → start
→ 不降低 Agent 对真实 Gateway health + authenticated Action probe 的合同
```

Targeted 验证：

```text
agent-runtime tests      26/26 PASS
agent-runtime typecheck  PASS
platform-cli tests       80/80 PASS
platform-cli typecheck   PASS
git diff --check         PASS
```

新增 Platform proofs：

```text
临时 runtime 按依赖拓扑启动 = PASS
Agent setup 失败仍清理本轮临时 runtime = PASS
原本已 RUNNING 的 service 不会被误停 = PASS
```

#### G. 三个真实 Custom GPT SOURCE_REAL 创建闭环

先用新 Agent Runtime 在 Fresh Workspace 物化空 owner credential store：

```text
BEFORE ACTION_REQUIRED
→ setup SUCCEEDED
→ AFTER READY
→ role-credentials.json = {} / 0 keys
```

随后用当前源码 Platform CLI 串行创建三个角色；没有并发，没有重复创建：

```text
研发 + 项目总控       PASS
运营 + 产品经理       PASS
部署 + 测试验收       PASS
```

每个 targeted Agent setup 的成功条件都包含：

```text
真实 ChatGPT Private Create
→ LIVE_CREATED
→ durable Role save
→ Gateway /health
→ Bearer Action probe
→ temporary runtime cleanup
```

本地 SOURCE_REAL 交叉证据：

```text
ROLE_COUNT                = 3
CREDENTIAL_KEY_COUNT      = 3
ROLE_CREDENTIAL_REF_MATCH = true
三个 Agent setupStatus     = READY
execution-runtime/model-runtime/platform-host/agent-gateway = STOPPED after cleanup
```

真实登录 Chrome 又逐一打开三个刚创建的 carrier 页面，Page Title 分别为：

```text
ChatGPT - 研发 + 项目总控
ChatGPT - 运营 + 产品经理
ChatGPT - 部署 + 测试验收
```

因此：

```text
CUSTOM_GPT_SOURCE_REAL_PASS = YES
```

注意：随后又进行了新的 Fresh reset，当前 `.proflow/agent/roles.json` 已不存在，所以这三个 SOURCE_REAL 结果只能证明修复链可行，**不能替代接下来 Registry-only Final Fresh 的三 Agent Gate**。

#### H. Agent/Platform release

源码 checkpoint：

```text
b8f1113  fix: close agent deployment bootstrap deadlock
```

版本化：

```text
@tomflow/proflow-agent-runtime 0.1.10 → 0.1.11
@tomflow/proflow-platform-cli  0.1.42 → 0.1.43
```

release metadata checkpoint：

```text
b874a42  chore: version agent deployment bootstrap fix
```

仓库当前 `release:status = No pending changes`，working tree clean。

本 Chat 最后曾启动 `/tmp/proflow-release-gate.log` 的 `release:sync:check + build + publishability` 批次；该 scratch 日志最后留下 `publishability [ELIFECYCLE] Command failed` 且 exit 文件缺失。**不要把这份 `/tmp` scratch 当 Registry 真值，也不要据此再次 publish。** 当前更高权威事实是：npm Registry 已存在 0.1.11/0.1.43，且本 Chat 已重新下载 exact tarball 验证修复代码确实在发布物中。若最终阶段仍希望补工程 publishability 证据，可在 Source repo clean 状态单独重跑并记录原因，但不要阻断或重复发布已存在 exact version。

### 0.3 当前唯一剩余目标

当前不是继续开发 Agent/Model，也不是继续 release。主线已经收敛为：

```text
Final Registry-only Fresh acceptance
→ Browser Fresh reality
→ Dev Tunnel Fresh reality
→ Model Fresh reality
→ 3 个 Custom GPT Fresh 真实创建
→ platform start
→ platform status = READY
→ stop → start
→ Browser / Tunnel / Model recovery
→ repeat / idempotency
→ no Fake READY / Fake SUCCESS
→ DEPLOYMENT_SUCCESS=YES
```

接管时首先机械核验当前 Workspace 是否已经是最终 controlled Fresh install。如果当前 `platform status=0/3`、23 modules 完整、关键 Registry version 与本节一致，可从 Browser Fresh Gate 继续；如果 Workspace 来源/Chrome registration/owner state 无法证明 Fresh，则先做一次受控 clean + Registry public install，再进入 Browser。不要为了“更干净”机械重复清理已经可证明的 Fresh 状态。

### 0.4 下一 Chat 禁止重复的动作

```text
禁止再次 publish agent-runtime@0.1.11
禁止再次 publish platform-cli@0.1.43
禁止重新研究 MR-01/MR-02/MR-03
禁止重跑 26/80 targeted tests，除非新源码再次发生变化
禁止拿 SOURCE_REAL 三 GPT 替代 Final Registry Fresh 三 GPT
禁止在 npm-owned Product Workspace 用 pnpm exec platform
禁止并发/高频重复创建 Custom GPT
禁止删除远端 Dev Tunnel
禁止 git push
禁止读取/打印任何 token/Bearer/API Key/账户身份
```

### 0.5 执行经验（本 Chat 新增）

1. **历史 PASS、SOURCE_REAL、REGISTRY_PASS、FRESH_REAL 必须分层命名。** 不能用“真实测试”笼统混在一起。
2. **外部持久状态是 Fresh 的一部分。** Workspace clean 不会自动清 Chrome registration、远端 Tunnel、浏览器登录态；必须按 owner/reality reset 或 revalidate。
3. **先判断产品是否仍在工作，再判断 harness 是否坏。** spinner/子进程活跃时不要不断换 matcher/PTY。
4. **同类 probe/状态采集矩阵化、跨文档更新批量化。** 实时反馈按验收步骤，不按 tool call 或文件数拆碎。
5. **Product Workspace 与 Source Repo 的 package-manager 语境必须分开。** npm-owned Product Workspace 只走本地 `platform` bin；Repo 工程才按 pnpm scripts。
6. **非幂等动作 UNKNOWN 先恢复权威状态。** 特别是 publish、Tunnel create、Custom GPT Create；禁止盲目重复。
7. **20 分钟左右开始收口一个执行批次，不再开新的大调查分支。** 最后留时间收集 exit/status/diff/断点；但这不是整个 Chat 的总时长限制，下一批可以继续执行。
8. **Deployment 的最终分母必须显式。** Custom GPT 创建已被用户裁决为必要 Gate，不能因为 Real-2 历史 PASS 或 Browser/Tunnel/Model 3/3 就提前宣布完成。

## 1. 当前阶段

```text
ARCHITECTURE = FROZEN
REAL_1 = PASS  # 历史结论；当前因新的部署 regression evidence 做部署重验
REAL_2 = PASS / FROZEN
REAL2_PROVISIONING_GO = YES
READY_FOR_REAL_3 = YES  # 历史阶段状态，当前不推进
CURRENT_EXECUTION_GATE = DEPLOYMENT_CLOSEOUT
DEPLOYMENT_SUCCESS = NOT_YET_RECONFIRMED
SYSTEM_REAL_USABLE = OUT_OF_SCOPE_FOR_CURRENT_GATE
PHASE3_FINAL_GO = NO
```

### 当前唯一目标：部署成功，不验证完整业务系统可用

当前这轮只闭环 **Deployment / Fresh Workspace**。部署成功不是单纯“命令 exit 0”，而是下面六个维度同时达到当前部署阶段要求：

**用户 2026-08-30 最新裁决：智能体创建属于部署阶段，且是 `DEPLOYMENT_SUCCESS=YES` 的必要条件之一。** 因此 Real-2 的历史 PASS 不得替代当前 Fresh Deployment 的真实创建 Gate；本轮必须从当前 Registry/Fresh 安装物完成真实 Custom GPT/智能体创建，并验证 ChatGPT 侧结果与本地 identity/config/evidence 闭环。

1. **技术闭环**：真实 Fresh Workspace 能完成 `platform install → platform setup → platform start → platform status`，部署必要状态成立且没有 Fake READY / Fake SUCCESS。
2. **CLI 产品质量**：输出、错误、进度、下一步和成功摘要合理；不把 23 个内部模块 traversal、shared facts、package CLI 等实现细节甩给普通用户。
3. **交互与用户心智**：一次只暴露当前真正需要处理的 root prerequisite；用户能理解“现在在哪、为什么停、下一步做什么”。
4. **自动化最大化**：机器能发现、创建、读取、验证、恢复的事实全部自动完成；正常只把 OAuth/2FA/验证码/真实 secret/不可替代决策交给人。
5. **部署稳定性**：重复 install/setup/start/status/stop、失败后重入、外部状态漂移和恢复行为稳定、幂等、fail-closed、可诊断，不制造不完整副作用。
6. **职责边界**：各 Package/Module 自己拥有真实状态与 lifecycle；Platform CLI 只做发现、排序、输入桥接、调用、聚合和用户呈现，不维护第二套业务真值。

必须严格区分：

```text
DEPLOYMENT_SUCCESS != SYSTEM_REAL_USABLE
```

当前 **不要求** J0～J6 业务 Journey、真实协作/Approval/Effect、Browser/File 实际业务任务执行、手机 FAST/THINK 联合执行等后续能力成立；这些属于后续 Real 阶段。只要部署合同本身成立，就应结束当前部署阶段并向后推进，禁止继续拿“系统是否真实完成业务任务”卡住部署。

固定路线不变，但当前先完成部署重验再继续：

```text
Real-1 Deployment / Registry / Fresh Workspace      HISTORICAL PASS / CURRENTLY REVALIDATING
→ Real-2 Real Custom GPT / Worker Identity          PASS / FROZEN
→ Real-3 Task Journey J0～J4                         WAITING FOR DEPLOYMENT RECLOSE
→ Real-4 Collaboration + Approval + Effect
→ Real-5 Browser / File / FAST-THINK Phone Model
→ Real-6 Recovery + J0～J6
→ J0～J6 REAL PASS → PHASE3_FINAL_GO = YES
```
## 2. 当前冻结的 Owner / Product 边界

每个 Module 对自己的真值负责：

```text
Module
├─ status()：真实观察自己的 setup/runtime 状态
├─ setup()：执行自己拥有的配置与恢复
├─ start()/stop()：拥有自己的生命周期
├─ 真实探测与证据有效性判断
├─ 状态/失败持久化
├─ 恢复建议
└─ READY / ACTION_REQUIRED / BLOCKED / FAILED 真值
```

Platform 只负责：

```text
调用 Module
→ 依赖/产品顺序编排
→ 收集用户输入并结构化传给 Module
→ 聚合 Module status
→ 找当前唯一根动作
→ 收敛普通用户输出
→ 做通用合同一致性保护
```

**禁止 Platform 替 Module 猜状态、补状态、伪造 READY、覆盖真实 FAILED，或实现 Module 专属业务真值。**

用户正式产品入口固定为：`platform install → platform setup → platform start`。普通用户不应执行 package-level `pnpm exec`、理解内部 moduleRef、Tunnel ID/port/shared facts 或模型内部字段。
## 3. 已冻结且不再重开的历史结论

以下只保留结论，不再保留 09～13 的过程流水账：

- Real-2 已最终 PASS；三个真实 Custom GPT Role、Knowledge ZIP、Action/Auth、Gateway probe、同 package 显式重建覆盖、rollback/queue isolation 均已有真实 evidence。
- 公共 `createCustomGptRole(...)` 与 `saveCurrentRole(...)` 语义已冻结；普通 setup retry 不重复创建 GPT。
- Real-2 除出现新的、可复现 regression evidence 外不得重开。
- `chatgpt-carrier` 已退休，不是 active surface；当前 active modules = 23。
- OpenAI Secure MCP Tunnel **不能**直接替换 Microsoft Dev Tunnel：前者是受控 MCP 通道，不满足当前 Custom GPT Actions → 公网 HTTPS REST Gateway 合同。Phase 3 保留 Microsoft Dev Tunnel。
- Model Provider 只认 generic OpenAI-compatible endpoint / credential / inventory；不得恢复 MLXHub、iPhone、Bonjour/mDNS、Ollama/LM Studio/vLLM 等设备/产品发现语义。
- FAST / THINK 必须来自真实能力 evidence；模型名称最多是合格后的次级信号。

这些事实只用于防止回退，不再作为当前执行任务。

## 4. 已完成的 Registry / Fresh 基线

当前 Fresh Registry 回放使用的关键版本：

```text
platform-cli                 = 0.1.42
execution-browser-extension  = 0.1.18
dev-tunnel                   = 0.1.20
devtunnel-cli                = 0.1.1  # dev-tunnel 内部依赖，不进入 23 Module surface
model-provider-api           = 0.1.17
model-runtime                = 0.1.19
agent-gateway                = 0.1.15
```

当前真实 Registry Fresh 已再次证明 23/23 active modules。Browser `0.1.18` 已按 Fresh 口径先卸载旧 Chrome registration 后重新 load unpacked 并完成 pairing；Dev Tunnel `0.1.20` 已 setup READY / host RUNNING。当前产品 Gate 已推进到 Model Provider / FAST / THINK。
## 5. 最近一次真实人工回放的前沿

当前根产品工作区的 Fresh Registry 连续回放前沿：

```text
Browser Extension = READY（Fresh 0.1.18，旧 registration 已先卸载后重装）
Dev Tunnel         = READY / host RUNNING（Fresh 0.1.20）
Model endpoint     = HTTP 200 / inventory 3 models
Model Provider     = 当前 Fresh Platform 尚未绑定
Model Runtime      = 等待 Provider binding / capability verification
platform status    = 2/3，当前步骤 Model Provider / FAST / THINK
PLATFORM_READY     = NO
```

真实模型外部 blocker 已解除；当前不是网络等待，而是需要继续模拟真人完成 `platform setup` 的 Provider Base URL 输入、inventory 验证和 Runtime FAST/THINK 能力门。普通 PTY 文本注入在该 prompt 上已经证明不稳定，后续按执行纪律切换到 `expect`/稳定交互 harness，不再重复尝试同一个失效 transport。

历史上曾出现的“Runtime 缺少合格 FAST 模型 / setup-status truth 丢失”等回归仍保留在后文 Root D/F，作为最终回归证据；不得把历史失败误写成当前 Fresh 实时状态。

Dev Tunnel 功能链当前真实 READY，不再是当前主 blocker。仍禁止为了验证幂等而盲目删除远端资源。

## 6. 当前源码接管现场

2026-08-30 最新机械读回：

```text
repo         = /Users/agent/Desktop/proton-workspace/repos/proflow
branch       = main
HEAD         = 041c592
working tree = 仅 05 / 09 / 10 三份公共上下文文档存在本轮未提交更新
business src = 当前没有新的未提交业务源码修改
```

前一轮 Browser / Dev Tunnel / Model Provider 的源码整改已经完成验证、version/publish，并进入当前 Fresh Registry replay；不能再把下面历史 WIP 描述当成当前未发布源码状态。后文保留的 Platform/Model/Browser/Tunnel WIP 条目只作为历史 root/evidence 说明，实时裁决以当前 Git、Registry、`10` 的 Gate 表和真实产品工作区为准。
### 6.1 Platform WIP

当前已有但未验证：

- 未安装 0 Module 时不再假 READY，并给 `platform install`；
- `steup` 等近似命令给出 `platform setup` 建议；
- `.proflow` 尝试写入 Git `info/exclude` 隔离；
- install/setup 的内部 23-module progress 改为可替换 detail，package-manager 普通 progress 被压缩；
- setup 交互改从 Module `actionRequired/data.candidates` 收 input，不再由 Platform 直接读取 Provider shared facts 猜模型列表；
- setup 结束后重新收集 Module status，用于产品级进度；
- status/setup/start/docs 默认输出开始收敛为 Browser → Tunnel → Model 三步用户 Journey；
- 默认 docs 改为产品帮助，不再列 23 份 Module 手册；
- 普通 help 隐藏 `setup/docs --module` 内部入口。

### 6.2 Model Runtime WIP

当前已有但未验证：

- 先对 inventory 做真实 capability probe，再生成角色候选；
- 只有多个**已合格等价候选**时才 ACTION_REQUIRED 让用户选择；
- probe exception 不再无痕吞掉，记录 `modelRef + reason`；
- 不合格候选记录具体 qualification reasons；
- setup 失败写入 owner `setup-failure.json`，status 可在相同 inventory fingerprint 下恢复真实 FAILED；
- 成功 mapping 后清除旧 failure；
- 用户术语统一 FAST / THINK，内部兼容字段仍可保留 `reason`。

### 6.3 Browser / Tunnel / Provider WIP

- Browser 已拆出 Developer Mode 独立确认 gate，随后才进入 Load unpacked/pairing。
- Browser/Tunnel/Provider 的恢复入口已统一指向 `platform setup/status`，正常文档不再要求 package-level `pnpm exec`。
- Dev Tunnel `No ports found` warning JSON 已兼容；真实 0.1.40 回放已证明 Tunnel 可进入 READY。
- Tunnel 当前 WIP 还在收敛 CLI 输出与恢复呈现；是否完全解决必须等最终验证。

### 6.4 2026-08-30 Fresh Browser 验收纠偏

最终 Fresh Registry replay 发现：Workspace clean 后，真实 Chrome 仍保留上一轮 `ProFlow Execution Browser 0.1.17` 的 unpacked registration；当前 Fresh 安装目录已经更新为 `0.1.18`。**此时只点击 Reload 会复用旧 Chrome registration，不能证明 Fresh Browser install。**

后续固定为：`确认旧产品扩展 → Remove/卸载 → 确认 registration 消失 → 从当前 Fresh Workspace 目录重新“加载未打包的扩展程序” → 验证版本/enabled/Service Worker/pairing/heartbeat/status`。Reload 只用于安装完成后的 runtime recovery/idempotency。此原则扩展到其它外部持久状态：Fresh 验收不能只清理 Workspace 文件，还必须识别浏览器 registration、外部进程、远端 owned resource、owner-local binding 等可能让新链路继承旧成功的状态，并按 ownership/安全边界做正确 reset/revalidate。

本次还确认了 Chrome 原生 UI 自动化边界：点击扩展卡片“移除”后出现的 `要删除“ProFlow Execution Browser”吗？` 是原生 AXWindow；普通 Playwright DOM 不足以完成确认。稳定方法必须保持 Chrome 前台，在同一原子系统脚本内用 macOS AX 读取当前按钮 bounds，再发送系统级鼠标事件，最后用 AX heading 消失 + `chrome://extensions` 截图双重确认。目录选择器同理按 macOS AX/系统键盘处理，不能因为弹窗关闭就假定动作成功。

本轮已经真实验证 macOS 卸载路径：Chrome AX 树能够直接识别 `AXHeading=ProFlow Execution Browser`、产品卡片“移除”按钮和 `AXWindow=要删除“ProFlow Execution Browser”吗？`；确认按钮是该原生窗口内的 `AXButton/description=移除`。稳定自动化不是固定屏幕坐标，而是 `activate Chrome → AX 取当前 bounds → CGEvent 鼠标点击 card remove → AX 取确认窗口按钮 bounds → 同一前台原子进程内 CGEvent 点击 confirm → AX + screenshot 复核 heading 消失`。确认浮层会在 Chrome 失焦时消失，因此跨工具调用先定位、后点击会漂移。2026-08-30 本轮最终复核：旧 `0.1.17` 产品卡已从真实 Chrome 消失，Fresh `0.1.18` 必须重新 load unpacked。
## 7. 当前未解决问题（按 Root，不再按历史流水账）

### Root A｜安装完整性与 Workspace 隔离

1. **Partial install 尚未真正识别。** 当前 WIP 只处理 `modules.length === 0`；还没有可靠区分：

```text
NOT_INSTALLED
INSTALL_INCOMPLETE
INSTALLED
```

若只安装部分 active modules，`status/setup/start` 仍可能进入错误路径。这是当前明确未闭环项。

2. `.proflow` Git 隔离已有新实现，但尚未经过跨 repo/root/subdir、已有 ignore、不可写 `.git/info/exclude` 等场景验证。
3. install 前后 23-module 内部进度已开始压缩，但是否达到“普通用户只看到一个安装过程”尚未真实验收。

### Root B｜Platform 聚合必须保持 Module truth

当前 `setupJourney()` 硬编码 Browser/Tunnel/Model 三个产品步骤，这可以作为**展示层**，但不得成为新的状态真源。

最终审计必须确认：

- Journey state 只由 Module status 结果推导；
- Module FAILED 不能被 Platform 改写成 BLOCKED/READY；
- 缺 Module / 部分安装不能被三步 Journey 隐藏；
- start/status/setup 的下一步必须来自真实 owning Module issue，而不是 Platform 猜测；
- 默认隐藏内部 moduleRef，不等于丢失诊断能力，详细模式/日志仍需可追溯。
### Root C｜Browser Extension READY 真实性

当前 Browser `status()` 仍主要依据：

```text
setup.json PRESENT
+ verification evidence PRESENT
+ extensionId 一致
→ setupStatus = READY
```

这仍可能把历史 evidence 当作当前真实运行状态。必须继续审计/整改：

- evidence freshness / 当前 heartbeat / service-worker reality；
- Extension 被卸载、禁用、重启或实例变化后的失效语义；
- 不能因为 Platform 想显示“步骤已完成”就保留 stale READY。

Developer Mode 与 Load unpacked 已在 WIP 中拆步，但最终仍需真实用户回放验证一次一个动作、取消、重入和 pairing。

### Root D｜Model Runtime 能力门与失败 evidence

当前 WIP 已经修正“原始 inventory 先选后验”的方向，但尚未验证。

仍需明确两个层次：

1. **实现问题**：资格验证、probe failure、rejection reasons、失败持久化、status 恢复必须通过测试和真实回放。
2. **合同问题**：FAST 当前要求 `text + vision + structured output + no-thinking + context>=16384 + maxOutput>=2048`。是否“FAST 必须 Vision”属于 Frozen 产品能力门，不能为了让当前模型通过而静默删除。

若要修改 FAST 必需能力集合，必须先按 `SPEC_GAP / CONTRACT_CHANGE` 裁决；如果合同不变，则实现只负责把“为什么不合格”说清楚。

### Root E｜Dev Tunnel 恢复与终端呈现

功能链已真实 READY；当前不再把 Tunnel 当主功能 blocker。

仍需闭环：

- 普通终端不能透传不必要的 Microsoft CLI 账户/原始噪音；
- 登录、create/reuse、port、host、HTTPS probe 应有可理解阶段进度；
- retry/idempotency 必须在受控条件下真实证明不会新增空 orphan；
- 当前远端资源不得在本批整改中自动清理。
### Root F｜状态摘要、恢复建议与文档一致性

当前 WIP 正在解决 setup/status 丢失 Model Runtime 根失败的问题，但最终必须同时证明：

- setup 本轮已完成 Browser/Tunnel/Provider 时，失败摘要不能退化成局部 `0/0/1`；
- status 必须能恢复最近仍有效的 Module FAILED；
- start 拒绝启动时只显示真实当前根问题，不把下游等待当根因；
- docs 默认不再要求用户从 23 个 moduleRef 中自己找答案；
- 正常产品文档只推荐 Platform 入口，FAST/THINK 术语一致；
- package CLI 可以保留为 owner 诊断入口，但不得作为正常 onboarding 的“下一步”。

### Root G｜非阻塞但仍需裁决/复核

- `task.sqlite-shm`：历史真实 observation 是 `platform status` 可能只改变 SHM mtime、内容/size 不变。当前不是 Real-3 blocker；是否把 auxiliary file mtime 纳入 strict pure-read contract 仍未裁决，禁止顺手修。
- 当前 WIP 文档和代码里仍有历史 package CLI 字符串/测试 fixture；需区分“诊断 API 合法存在”与“普通用户 surface 泄漏”，不能用全局字符串删除破坏 owner 工具。

### 7.1 本 Chat 后半轮状态机回放新增的必须回归证据

以下是在 `09` 初版之后继续真实 replay 暴露的事实，下一 Chat 不得丢失；它们优先并入既有 Root，而不是再造一套问题树：

- **Root A / F：0-module 假成功**：Fresh/uninstalled 状态下，`platform status` 曾 RC0 + `PLATFORM_READY=YES`，`platform start`/`setup` 也曾 RC0 假成功；stop 只是 safe no-op。当前 WIP 声称已处理 0 Module，必须真实回归。
- **Root A：partial install**：只安装一个 Module 时 `status` 曾继续 fake READY，而 setup/start 能因依赖缺失 fail-closed；说明完整性 gate 不能只在某些命令存在。
- **Root A：混合 package manager / 非原子 uninstall**：npm 安装后的 workspace 执行公开 `pnpm exec` 曾把 23 个包移动到 `node_modules/.ignored` 并产生 pnpm 文件；`install` 能报 `PACKAGE_MANAGER_CONFLICT`，但 status/setup/start 曾绕过；更严重的是 uninstall 先调用 23 个 Module uninstall，再最终报 conflict，留下 manifest/node_modules/.proflow/locks 矛盾状态。必须验证“先 preflight、后 mutation”。
- **Root D/F：Provider exit code 不一致**：真实不可达 Provider URL 场景曾出现 setup stdout 明确 unavailable，但进程 RC0。用户可见失败语义与 exit code 必须一致。
- **Root D/F：Runtime setup/verify 自相矛盾**：package runtime setup 曾 RC0/声称 FAST+REASON READY，但紧接 verify RC1 BLOCKED waiting Provider；Platform targeted runtime setup 也出现 stdout 显示 READY 能力但命令 RC1。setup/status/verify 必须共享 owner truth。
- **Root D：Runtime PTY 交互链断裂**：候选选择时文本出现“请选择 REASON 候选”，Enter/选择后进程直接结束，没有继续 FAST，mapping 不落盘，verify BLOCKED；显式 fast/reason 参数路径反而成功。必须真实 PTY 回归，而不能只测参数 API。
- **Root C：Browser stale READY 风险**：真实 ProFlow Browser evidence 曾停留在旧 `PAIRING_HEARTBEAT` 时间点，而 Platform 仍把 Browser 算“配置完成”。这仍只是需要结合 freshness contract / live heartbeat 做确认的高风险 candidate，不能仅凭文件年龄直接宣判 bug。
- **Root F：start-only service 裸栈**：部分 service bin 在缺配置时曾直接抛 Node `ENOENT` stack，属于普通用户 CLI 错误呈现需要收敛的部署 UX 回归项。

运行期 scratch issue log 曾使用 `/tmp/proflow-real3-issues.md`；它不是规范真源。下一 Chat 若继续使用，应先读回再将稳定 root/evidence 回写本文，不能只依赖 `/tmp` 持久化。

## 8. 历史 P1～P38 到当前 Root 的最小映射

只保留对回归仍有价值的编号：

| 历史问题 | 当前状态 |
|---|---|
| P1 / P22 install 前后 23 项刷屏 | Root A，WIP 已改 progress retention，待验证 |
| P2 gateway 依赖过度报告 | 已在 0.1.40 前修复并真实回放，不再作为当前问题 |
| P3 旧 Fresh Git 混乱 | 历史测试现场已恢复，不再作为当前 blocker |
| P4 / P36 / P37 docs 暴露内部模块/命令 | Root F，当前 WIP 继续收口 |
| P5 / P6 / P9 onboarding/targeted scope | 主功能顺序已在 0.1.40 回放成立；默认呈现继续归 Root B/F |
| P7 / P8 / P27 / P28 Browser 安装交互 | Root C，WIP 拆步，待真实回放 |
| P10 Browser pre-start unhandled fetch | 已在此前候选收口，不再列当前主问题；最终 Browser 回放顺带防回归 |
| P11 Dev Tunnel empty-port JSON | 已修且真实 Tunnel READY |
| P12 / P29 Tunnel 恢复与终端噪音 | Root E，功能 PASS、呈现待闭环 |
| P13～P17 Platform input bridge / package CLI / 双包管理器 | 正常产品路径已在 0.1.40 收口并真实单 pnpm Fresh PASS；继续由 Root F 防 package CLI 回漏 |
| P18 SQLite SHM mtime | Root G，非 blocker，合同未裁决 |
| P19 0 Module 假 READY | Root A，当前 WIP 只修 0 Module，partial install 仍未闭环 |
| P20 命令拼写恢复 | 当前 WIP 已实现建议，待测试 |
| P21 `.proflow` 未 Git 隔离 | Root A，新增 `git-isolation.ts`，待验证 |
| P23 / P24 / P25 / P26 / P30 内部模块与错误用户进度 | Root B/F，当前 WIP 正在收敛 |
| P31 原始 inventory 直接给用户选 | Root D，当前 WIP 改为能力验证后仅返回合格歧义候选 |
| P32 capability probe 失败原因丢失 | Root D，当前 WIP 记录 failures/rejections，待验证 |
| P33 / P34 setup/status 丢失真实根失败 | Root F + Model Runtime failure persistence WIP |
| P35 中英文混用 | 当前 interaction WIP 已关闭 select instructions，仍需真实 TTY 验收 |
| P38 docs“先验证后选择”与实现相反 | Root D/F；当前 WIP 正在让实现与文档重新一致 |

未列出的旧编号表示已被更晚真实证据覆盖，或只是上述 Root 的重复表现，不再单独驱动整改。

## 9. 下一 Chat 的长期执行模式：一直做到 Deployment PASS 或上下文极限

下一 Chat 不再按“执行一条命令 → 汇报 → 等用户确认”的节奏工作。用户已授权在当前 Deployment Closeout 边界内持续执行，目标是**尽可能独立跑到部署成功**；只有真正不可替代的人类动作、越权事项或上下文即将耗尽时才中断。

固定循环：

```text
A. 恢复权威现场
→ branch / HEAD / dirty WIP / Registry / product workspace / runtime / MCP

B. 完成一轮高覆盖率真实部署发现
→ 真实 /Users/agent/Desktop/proton-workspace
→ CLI 状态机 + PTY + Playwright Chrome + evidence/process/filesystem
→ 发现问题先记录，能安全继续就继续，不因第一个失败中断整轮

C. 冻结完整 issue/root set
→ 按 owner/root cause 归并，不按表面报错数量修

D. 批量整改已授权 Deployment roots
→ CodeGraph 先定结构/blast radius
→ Local Dev 验证当前源码
→ 同构机械改动脚本/codemod 优先
→ 一个 root 涉及多个文件就一次完整修改

E. 批次末统一工程验证
→ format / scoped lint / affected tests / typecheck / build / architecture / publishability / diff check

F. 有产品源码修改则真实发包
→ exact Registry preflight/readback
→ UNKNOWN/timeout 先恢复权威状态，禁止盲目重发

G. 脚本化清理真实产品工作区
→ 只删除部署安装/运行痕迹，保护 repos/ 与源码 dirty WIP
→ 从真实 Registry Fresh install

H. 重新真实 setup/start/status
→ Local Dev + Playwright Chrome 交叉观察
→ 若仍有部署问题，回到 B
→ 六个 Deployment Success 维度全部成立才 PASS
```

### 9.1 效率规则

- **脚本优先**：重复、机械、确定的动作（workspace clean、Registry readback、状态采集、CLI 状态机、hygiene check）优先沉淀为安全脚本/harness，不反复用几十次 MCP 调用模拟手工。
- **上下文/文档同步默认批处理**：同一规则或事实涉及多个公共上下文文件时，先一次性搜索全部目标并形成变更矩阵，再一次脚本化修改、一次统一校验；禁止逐文件 read/edit/read 串行更新。
- **实时反馈不允许反向制造碎片化执行**：一次跨文档批处理、一次状态采集批次、一次 harness 执行都各算一个“有意义步骤”；只在该批次结果成立后反馈一次，不按底层 tool call 数量刷屏。
- **短批次，防单次工具任务卡死**：一次只启动有限、可恢复的任务；长进程记录 PID 后合理间隔读回。不要用一个超长 shell/测试链把整轮塞进单次工具调用。**这不限制整个 Chat 的连续执行时长：短批次完成后必须直接进入下一短批次，不存在执行 25/30 分钟后主动停工的规则。**
- **正常路径低采样，异常路径高采样**：Playwright Chrome 是“眼睛”，但不机械每一步截图；关键浏览器状态转换观察，异常时再增加截图/Console/Network 采样。
- **测试最后统一跑**：发现阶段不边找边全量 test；先冻结完整 root set、批量整改，再统一验证。
- **发布按整改批次，不按单 Bug**：一轮发现 → 一轮整改 → 一轮验证 → 一次版本/发布 → Fresh replay，避免 Registry 变成逐 Bug 日志。
- 自动创建 Custom GPT 的 Real-2 历史能力证据已真实 PASS；但 **Custom GPT / 智能体真实创建仍是当前 Deployment 最终 PASS 的必要 Gate**。本轮 Fresh Registry 必须至少执行一次受控真实创建并核对 identity/config/evidence 闭环；为避免账号风控，不做无意义高频重复创建。

### 9.2 汇报规则

**最新用户要求覆盖此前“不要过程汇报”的旧指令：Deployment Closeout 现在必须逐步实时反馈。** ChatGPT 当前以“模拟真实人做部署验收”的方式执行，每完成一个有意义的用户可感知步骤，就立即反馈该步骤的动作、真实结果和下一步，然后直接继续，不等待用户确认。

固定节奏：`执行步骤 → 实时反馈 PASS/FAIL/DOING + 关键事实 → 立即继续下一步骤`。反馈不是 checkpoint，不触发额外 test/format/full gate，也不能成为主动停工理由。底层为同一验收步骤服务的多次只读 tool call 可以合并，不要求每个 tool call 都刷消息；但 Browser remove/load/pairing、Tunnel setup/recovery、Provider inventory、FAST/THINK 映射、platform start/status、stop/restart、uninstall/reinstall 等有意义状态转换必须分别反馈。

真正允许中断连续执行的条件仍只有：`DEPLOYMENT_SUCCESS=YES`、不可替代的人类 OAuth/2FA/验证码/secret/外部授权、Frozen Contract/Owner/Architecture 变更、明确不可恢复工具失败、或真实上下文硬极限。实时反馈不改变这些中断条件。

如果某项真实模型能力、网络或外部服务卡住，但不属于部署 PASS 必须证明的后续业务能力：记录证据并实时说明，能最小绕过则绕过，继续其它部署验证，不得无限卡在该点。

任何实现若需要改变 FAST 必需能力集合、Frozen Owner/Contract/Architecture，立即 STOP 为 Contract Change；不得为了让当前环境通过而暗改合同。
## 10. 当前真实资源保护边界

接管时最近已知远端 Dev Tunnel 总数为 5：1 个当前有效 owner、4 个 `portCount=0`。历史明确记录的空 Tunnel 至少包括：

```text
swift-fog-skzzjw8.jpe1
swift-chair-28k7rl1.jpe1
quick-chair-9rv1scq.jpe1
```

另有历史 `ai-agent-platform-mvp.eun1`。不要根据旧列表猜第五个资源身份；需要时只读机械重查。

当前资源保护边界：

- 不 delete 任何远端 Tunnel；
- 不为了幂等测试盲目重跑会 mutation 的 setup；
- 不 reset/clean 当前 dirty source working tree；
- 不读取/打印 token、Bearer、API Key、账户身份；
- Deployment Closeout 范围内为验证真实 Registry 安装物所需的 `npm publish` 已获授权，但 UNKNOWN/超时必须先 exact readback，禁止盲目重发；
- 不 git push。

## 11. 当前用户授权与真实部署输入

用户已明确授权当前 Deployment Closeout 采用**真实环境长期迭代**，不是临时 Workspace / Mock 环境：

- 真实产品工作区：`/Users/agent/Desktop/proton-workspace`；需要 Fresh replay 时清理的是部署工作区安装/运行痕迹，不得 `reset/clean` 源码仓库 dirty WIP。
- OAuth / GitHub 登录当前视为仍有效；自动化流程可直接连接和复用。若后续真实过期，再只在不可替代的本人授权节点请求用户介入。
- 真实 Model Provider endpoint 固定输入：`http://192.168.0.108:8080/v1`。部署测试优先使用该真实 endpoint，不再用临时 mock provider 代替最终部署验收。
- 若真实模型能力/网络/外部状态卡住，按 `记录问题 → 能绕过则最小绕过 → 尽可能继续完成其余自动化测试与整改` 执行；不得把后续“业务系统真实可用”能力反向变成部署 PASS 的门。
- 当前允许在 Deployment Closeout 已冻结范围内自主完成：真实自动化测试、CodeGraph/源码根因分析、冻结完整 issue/root list 后批量修改源码、批次末统一工程验证、重复真实部署回放和交互调优。
- **有源码修改且需要验证真实 Registry 安装物时，已授权真实发包**；发布后清理真实部署工作区并继续 Fresh 测试。发布前后仍要 exact Registry readback，UNKNOWN 时禁止盲目重复 publish。
- 自动创建 Custom GPT/智能体的链路此前已真实 PASS，但不能作为当前 Deployment Gate 的替代证据。当前部署验收必须通过本轮 Fresh Registry 安装物真实创建智能体并核对 ChatGPT 侧结果与 owner-local identity/config/evidence；调用频率保持最低必要次数，禁止高频重复创建。
- `npm publish` 已包含在上述当前部署迭代授权内；`git push`、删除远端 Dev Tunnel、改变 Frozen Contract、进入部署后的业务系统真实可用测试仍不在自动授权范围。

### 11.1 本 Chat 已恢复的本机自动化工具链

`gptweb-mcp` 已统一管理 `local-dev / codegraph / playwright-chrome`，稳定方法见 `05`。本 Chat 最近一次完整确认曾得到：

```text
LOCAL_DEV=READY
CODEGRAPH=READY
PLAYWRIGHT_CHROME=READY
GPTWEB_MCP=3/3_READY
```

Playwright Chrome 已通过 extension mode 连接用户正在使用的**真实 Chrome**，不是 Chrome for Testing；它是当前真实 Browser/Extension/E2E 验收的默认“眼睛 + 浏览器里的手”。不要为了测试默认重启真实 Chrome 或改走旧 `127.0.0.1:9229` CDP 路线。

必须区分两个扩展身份：Playwright MCP 自己的连接扩展曾为 `mmlmfjhmonkocbjadbfplnigmagldckm`；ProFlow Browser Extension 最近 evidence 中的 extensionId 为 `eehdadpmjffomabiedcjijiakconalab`。二者绝不能混为同一个产品扩展。Playwright 的连接 URL 可能含 relay token，禁止输出/落盘 token。

自动化分工固定为：`CodeGraph → 结构/调用链/blast radius`；`Local Dev → 当前源码/文件/命令/Git/进程/evidence/测试/批量修改`；`Playwright Chrome → 真实页面/Tab/Extension/UI/Console/Network/可见结果`。浏览器异常不能只靠 CLI/evidence 猜，CLI READY 也不能替代真实页面 observation。

`chrome://extensions` 等 Chrome 内部页通过 Playwright 可能 detach/不受支持，这属于工具/Chrome internal page 限制，不是 ProFlow 产品 bug；应改用正常页面、目标扩展 UI、精确本机 profile/bridge/evidence 读取等安全路径验证。

## 12. 执行纪律

继续遵守 `05-执行纪律与工具规则.md`：

```text
ISSUE_DISCOVERY != FIX_AUTHORIZATION
先审完整范围
→ 冻结 residual/root list
→ 一次完成已授权整改
→ 最终统一验证
→ 原真实失败场景重放
```

当前已获得 Deployment Closeout 范围内的持续执行授权：在完整 issue/root list 冻结后，可自主批量整改、统一工程验证、真实发包、清理真实部署工作区并继续 Fresh replay，直至部署 PASS；无需针对同一部署范围内每个 Bug 再次逐项申请。`ISSUE_DISCOVERY != FIX_AUTHORIZATION` 继续用于防止越界：新的后续业务能力、Frozen Contract 变更、远端 Tunnel 删除、git push 等仍需单独授权。不得自行扩到新的 Real-3 Journey、Real-4/5/6 或重开 Real-2。

## 13. 新 Chat / 新 Work 最小读取顺序

1. 本文 `09-Real3当前上下文与未解决问题-20260829.md`。
2. `10-Deployment-Closeout循环测试进度计划书.md` —— 当前 Round / Gate / residual / Next Action 的实时执行总控。
3. `02-当前总控状态与Real路线.md`。
4. `05-执行纪律与工具规则.md`。
5. `03-冻结架构与关键决策.md` + `04-测试验证与验收方法.md`。
6. 当前 dirty working tree / Git diff / 对应正式领域 spec 与 Test Plan。

**不再需要读取旧 09～17 的历史交接链。** 历史原因需要追溯时使用 Git history，而不是把旧 snapshot 重新带回当前上下文。


## 14. 下一 Chat 直接复制使用的接管提示词

```text
项目：ProFlow Phase 3 / Deployment Closeout
源码仓库：/Users/agent/Desktop/proton-workspace/repos/proflow
真实产品工作区：/Users/agent/Desktop/proton-workspace

当前唯一目标：完成 Final Registry-only Fresh Deployment Acceptance，直到 DEPLOYMENT_SUCCESS=YES。不要推进部署后的 Real-3/4/5/6 业务 Journey。

第一步读取：
1. spec/平台架构与公共约定/00-公共上下文/README.md
2. 09-Real3当前上下文与未解决问题-20260829.md —— 尤其先完整读“## 0. 2026-08-30 本 Chat 最新收口”
3. 10-Deployment-Closeout循环测试进度计划书.md
4. 02-当前总控状态与Real路线.md
5. 05-执行纪律与工具规则.md
6. 03-冻结架构与关键决策.md + 04-测试验证与验收方法.md

接管后先机械恢复，不准猜：
- git branch / HEAD / git status
- npm Registry exact version / exact tarball
- Product Workspace 当前 package versions / package-manager ownership
- ./node_modules/.bin/platform status
- Chrome 中 ProFlow Browser Extension 当前 registration / enabled / Service Worker
- Dev Tunnel 本地 host 与 owner state（不删除远端资源）
- Model endpoint http://192.168.0.108:8080/v1 可达性
- .proflow/agent roles.json / credential key count（只读 key 数和 roleRef 对应，不输出 credential）

最近机械真值（必须重读确认）：
Source HEAD = b874a42
working tree = CLEAN
Registry 已存在并 exact tarball 验证：
- @tomflow/proflow-agent-runtime@0.1.11
- @tomflow/proflow-platform-cli@0.1.43
- @tomflow/proflow-model-runtime@0.1.20
Product Workspace 最近已安装：
- platform-cli 0.1.43
- agent-runtime 0.1.11
- browser-extension 0.1.18
- dev-tunnel 0.1.20
- model-provider-api 0.1.17
- model-runtime 0.1.20
- agent-gateway 0.1.15
- 三 Agent package 0.1.15
最近 public platform status = 0/3，Browser 当前步骤，0 个服务进程，PLATFORM_READY=NO；roles.json=MISSING，credential store=0 keys。

不要再次 publish agent-runtime@0.1.11 或 platform-cli@0.1.43。这两个 exact Registry tarball 已重新下载验证，确实包含 Agent owner role-store 初始化和 Platform temporary dependency bootstrap 修复。历史 /tmp/proflow-release-gate.log 的 publishability ELIFECYCLE 是 scratch 现场，不是再次 publish 的理由；如最终需要工程证据，只在 repo clean 后单独重跑 publishability，不得重复相同 npm version。

当前最终 Journey：
Registry exact artifacts
→ controlled Fresh Workspace（先确认当前是否已经满足，不机械重复 clean）
→ public platform install/status
→ Browser Fresh：旧 registration 必须 Remove，当前 Fresh 目录重新 Load unpacked，验证 version/enabled/Service Worker/pairing/live heartbeat
→ Dev Tunnel Fresh：auth/reuse-or-create/host/HTTPS probe/READY，不删除远端 Tunnel
→ Model Fresh：Provider URL=http://192.168.0.108:8080/v1，inventory → Runtime FAST/THINK → READY
→ Custom GPT Fresh 创建：研发+项目总控、运营+产品经理、部署+测试验收；必须真实 ChatGPT 页面 + durable roles + credential refs 1:1 + Gateway health/Bearer Action probe
→ platform start
→ platform status = READY
→ stop→start / repeat setup/start / Browser disable-reload / Tunnel host-down recovery / Model fail-closed-recovery
→ final status/evidence
→ DEPLOYMENT_SUCCESS=YES

重要历史已完成，不要重开：
- Model Runtime MR-01/02/03 已修，0.1.20 Registry exact PASS，SOURCE_REAL setup/start/status/stop PASS。
- Agent/Gateway Fresh lifecycle deadlock已修：agent-runtime 0.1.11 初始化 owner durable store；platform-cli 0.1.43 在 agent-package setup 前临时 bootstrap dependency closure，并只清理本轮临时启动 runtime。
- affected tests 已过：agent-runtime 26/26 + typecheck；platform-cli 80/80 + typecheck；git diff --check PASS。
- SOURCE_REAL 已串行真实创建 3 个 Custom GPT，并用真实登录 Chrome 打开验证标题；但随后已 Fresh reset，所以这只能证明修复链可行，不能替代最终 Registry Fresh 三 GPT Gate。

产品 Workspace 执行纪律：
- Product Workspace 是 npm-owned 时，禁止 `pnpm exec platform ...`；它曾把 23 包移到 node_modules/.ignored。使用 `./node_modules/.bin/platform` 或 npm-owned 等价公开入口。
- Source Repo 工程命令继续遵循仓库 pnpm scripts；不要混淆两个语境。
- publish/Tunnel create/Custom GPT create 等非幂等动作如果 UNKNOWN，先恢复外部/文件/进程权威状态，绝不盲重试。
- 不读取或输出 token/Bearer/API Key/账户身份。
- 不 git push，不删除远端 Dev Tunnel，不 reset/checkout/clean 源码仓库。
- 不改变 Frozen Contract/Owner/Architecture；如果真的必须改合同才停下来找用户。

工具分工：
CodeGraph → structure / caller-callee / ownership / blast radius
Local Dev → 当前源码 / Git / 文件 /进程 / Registry / tests / 批量脚本
Playwright Chrome → 真实 Chrome 页面 / Tab / Custom GPT / 普通 Web UI / Console / Network
chrome://extensions 与 macOS 原生确认框/目录选择器优先 macOS AX/系统自动化，不把 Playwright internal-page detach 当产品 bug。

执行效率：
- 一次状态批量读取，一次 root 批量处理，一次统一验证；不逐文件/逐命令碎片化。
- 实时反馈按“有意义验收步骤”反馈：做了什么 / PASS-FAIL-DOING / 下一步；反馈后直接继续，不等待用户确认。
- 长进程一次启动、落 PID/log/exit，低频 readback；无输出/Blocked != hard blocker。
- 约 20 分钟开始收口当前大批次，留最后几分钟做权威 readback 和断点落盘；这不是整个 Chat 的总执行上限。

只有以下情况允许停：
1. DEPLOYMENT_SUCCESS=YES；
2. 必须用户本人完成 OAuth/2FA/CAPTCHA/secret/外部授权；
3. 必须改变 Frozen Contract/Owner/Architecture；
4. 明确不可恢复工具失败；
5. 真实上下文硬极限并已安全落盘。

现在直接执行，不重新讲一遍计划，不等待确认，一直把 Final Registry Fresh Journey 做完。
```
