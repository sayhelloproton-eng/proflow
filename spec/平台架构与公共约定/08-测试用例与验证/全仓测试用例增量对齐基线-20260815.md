# ProFlow 全仓测试用例增量对齐基线｜2026-08-15

> 性质：基于最新版领域/技术/测试文档，对原有测试资产做吸收、更新、补全与历史清理。不是重建。代码不定义 Expected。

## 1. 数量

- 24 packages：**24/24 已逐包审计更新**
- 5 Bounded Context：**5/5**
- 4 formal services：**4/4**
- Module Test Plan formal cases：**261 = 159 CP + 102 RF**
- External Resource package cases：**15**
- Cross-domain/Journey/Observer/Recovery/Approval/E2E acceptance cases：**44**
- Executable test files：**66 → 105**
- Executable `test()` cases：**444 → 541**

> **Batch 6 口径澄清**：以上 `261`、`44`、`541` 都是 **2026-08-15 历史 trace/source inventory 数量**，不是当前 PASS 数。`261` 表示当时 formal identity inventory；`44` 表示 cross-domain trace/evidence mapping，不等于 44 条独立真实 runtime/E2E journey；`541` 表示当时源码中的 `test()` 调用数量，不等于 541 条已执行 PASS。Batch 6 当前机械统计与 proof-strength 分类以 `BATCH6-TEST-GOVERNANCE-RECONCILIATION.json` 和重生成的 `EXECUTABLE-TEST-INVENTORY.json` 为准。

> **Batch 6 当前机械盘点（2026-08-16）**：37 Test Plans；312 formal identities = 188 CP + 124 RF；122 executable test files；634 `test()` source calls；44 cross-domain trace/evidence mappings；`NO_MAPPING=0`。这些是**源码映射事实**，仍不等于自动化 PASS；真实外部项继续保持 `REAL_EXTERNAL_PENDING`。

## 2. 真源纪律

```text
最新版领域文档 + 技术文档 + 测试文档
→ 定义 Expected / Forbidden / Evidence
现有测试 → KEEP / UPDATE / ADD / REMOVE_HISTORICAL
现有代码 → 只用于 executable landing point，不可反向修改 Expected
```

## 3. 机器可读入口

- `REPOSITORY-TEST-CASE-MATRIX.json`：2026-08-15 历史 261 formal identity trace baseline；已由 Batch 6 reconciliation supersede。
- `EXECUTABLE-TEST-INVENTORY.json`：当前全部 executable `test()` source inventory（不表示 PASS）。
- `BATCH6-TEST-GOVERNANCE-RECONCILIATION.json`：当前 Test Plan / Formal CP-RF / executable source / proof class / cross-domain trace 的机械 reconciliation。
- `PACKAGE-TEST-COVERAGE.json`：24/24 package hard ledger。
- `SERVICE-TEST-COVERAGE.json`：4/4 formal services。
- `BOUNDED-CONTEXT-TEST-COVERAGE.json`：5/5 BC。
- `CROSS-DOMAIN-ACCEPTANCE-CASES.json`：J0→J6、Observer、Native GPT、Recovery、Approval、Security、Final Manual E2E。
- `HISTORICAL-TEST-DISPOSITION.json`：历史用例删除/改标/替代链。

## 4. Real External 纪律

真实 Chrome / Custom GPT / File Bridge / Always Allow / 手机 Model / Dev Tunnel / Provider reality 只能在最终真实环境给 PASS；本地 static/mock case 只能证明 contract/handoff，不得 fake E2E。
