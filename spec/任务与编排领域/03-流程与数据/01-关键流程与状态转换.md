---
docId: TASK-DOC-03-01
title: 任务与编排领域｜关键流程与状态转换
docType: flows
authority: normative
lifecycle: active
domain: task-orchestration
subdomain: null
subdomains:
- task-lifecycle
- task-chain
- node-workflow
provides: []
requires: []
contractRefs: []
---

# 任务与编排领域｜关键流程与状态转换

---

# 1. 正常 Task 主流程

```text
Task PENDING
   ↓ human authorization（独立 Task；TaskGroup 成员可由已授权 TaskGroup 满足该门）
   ↓ 前置条件满足
Task READY
   ↓ provision / 补齐 required TaskRoleBinding
   ↓ startTask
Task ACTIVE
   ↓
Node-1 READY
   ↓ startNode（Task 自动解析 TaskRoleBinding）
Node-1 IN_PROGRESS
   ↓ completeNode
Node-1 SUCCEEDED
   ↓
Node-2 READY
   ↓
...
最后 Node SUCCEEDED
   ↓
Task SUCCEEDED
```

第一版不引入自动 Graph Engine。下一 Node 按 `sequenceNo + 1`。

---

# 2. createTask

`createTask` 一次完成 Task + planVersion + ordered Nodes + initialDocuments，不拆 `createTask / createPlan / attachPlan`。

**新 Task 一律先进入 PENDING。** Human authorization 是独立业务门；批准后 Task 才进入 READY。若属于 TaskGroup 且前序条件未满足，则即使已批准也继续保持不可启动，直到 Task Owner 判定其满足 READY 条件。

---

# 3. startTask

原子效果：

```text
校验 Task == READY
校验 expectedTaskVersion
校验 group eligibility（如有）
校验 required TaskRoleBinding 已补齐

Task READY → ACTIVE
Task.version + 1
Task.startedAt = now

第一个 Node PENDING → READY
Node.version + 1

Task.currentNodeId = firstNodeId

写 Event
写 idempotency record
COMMIT
```

不得出现 Task 已 ACTIVE 但 first Node 仍 PENDING。

---

# 4. startNode

调用前必须已经存在 `TaskRoleBinding(requiredRoleRef → workerRef)`。调用方不再为 `startNode` 选择任意 workerRef。

原子效果：

```text
校验 Task == ACTIVE
校验 Node == READY
按 Node.requiredRoleRef 读取 TaskRoleBinding
校验 binding 存在且有效
校验 expectedTaskVersion
校验 expectedNodeVersion
校验 idempotency

Node.workerRef = TaskRoleBinding.workerRef
Node.status = IN_PROGRESS
Node.startedAt = now
Node.version + 1

Task.version + 1
NODE_STARTED Event
COMMIT
```

第一版没有 CLAIMED 中间状态。

---

# 5. completeNode

前置：Task ACTIVE、Node IN_PROGRESS、actor / worker 与当前 workerRef 合法、expectedVersion 正确、必需 outputDocuments 已存在。

事务：

```text
保存当前 run 的 execution history
Node → SUCCEEDED
Node.completedAt = now
Node.version + 1

若有下一个 Node：
  next PENDING → READY
  next.version + 1
  Task.currentNodeId = nextNodeId

若无下一个 Node：
  Task → SUCCEEDED
  Task.currentNodeId = null
  Task.completedAt = now

Task.version + 1

NODE_COMPLETED
NODE_READY 或 TASK_SUCCEEDED
idempotency record
COMMIT
```

---

# 6. waitNode

用于业务确认、Action 权限、Browser / Execution 异常或无法安全继续。

请求包含 `waitType / reasonCode / message / relatedRef?`。

效果：

```text
Node → WAITING
Task → WAITING
创建 PendingMessage
写 NODE_WAITING / TASK_WAITING Event
```

整个 Task 停止。

---

# 7. resumeTask

用于 `PAUSED → ACTIVE` 或 `WAITING → ACTIVE`。

若 current Node 是 WAITING：`Node WAITING → IN_PROGRESS`，runNo 和 workerRef 不变。

如果只是 Task PAUSED，Node 状态无需变化。

FAILED 不走普通 resume，优先显式 reopen。

---

# 8. failNode

只用于技术执行失败：

```text
Node → FAILED
Task → FAILED
记录 errorCode / errorMessage / retryable
创建必要 Event / Message
```

不自动重试。修复后 reopen 当前 Node，开始新的 runNo。

---

# 9. pauseTask

```text
Task ACTIVE / WAITING / FAILED
→ PAUSED
```

不逐个修改 Node。

所有执行方下一次推进前必须检查 Task.status。Task PAUSED 时禁止 startNode / completeNode / 新业务推进，扩展停止自动驱动。

---

# 10. terminateTask

```text
Task → TERMINATED
```

未完成 Node 可 TERMINATED；已 SUCCEEDED Node 保留；不可 resume。

---

# 11. reopenNode

例：

```text
1 产品       SUCCEEDED
2 研发       SUCCEEDED
3 测试       WAITING
4 上线       PENDING
```

执行 `reopenNode(node-dev)`：

```text
校验 Task 当前允许 reopen
校验目标 Node 属于当前 Task
校验 expectedTaskVersion

目标 Node:
  runNo + 1
  status = READY
  workerRef = null
  startedAt = null
  completedAt = null
  result / error 清空
  version + 1

所有 sequenceNo > target 的后续 Node:
  status = PENDING
  workerRef = null
  startedAt = null
  completedAt = null
  当前 result / error 清空
  version + 1

Task:
  status = ACTIVE
  currentNodeId = target
  version + 1

旧 history / document / event 全保留
NODE_REOPENED Event
COMMIT
```

原则：reopen 点以前 Node 保持已完成；reopen Node READY 新 run；之后 Node PENDING 等待重新经过。

---

# 12. 为什么不做通用循环

若直接允许 TEST → DEV → TEST → DEV，会立即引入循环计数、退出条件、旧 Node 状态、多轮 Artifact、死循环、自动回边、恢复点等复杂度。

第一版：跨 Node 回退 = `reopenNode`；Node 内多轮业务讨论 = Worker 自己处理。

---

# 13. 测试不通过推荐流程

```text
研发 Node SUCCEEDED
        ↓
测试 Node IN_PROGRESS
        ↓
测试工作执行成功
        ↓
写 test-result.md
业务结果 = NOT_PASSED
        ↓
waitNode(BUSINESS_CONFIRMATION, TEST_NOT_PASSED)
        ↓
Task WAITING
        ↓
总控读取测试结果
        ↓
人工 / 总控确认
        ↓
acknowledgeMessage
        ↓
reopenNode(研发)
        ↓
研发 runNo + 1
        ↓
重新向后推进
```

`TEST_NOT_PASSED != Node FAILED`。

---

# 14. Action 权限 / Browser 异常

```text
Execution / Browser detects approval required
        ↓
waitNode(ACTION_APPROVAL_REQUIRED)
        ↓
Task / Node WAITING
        ↓
PendingMessage
        ↓
人工处理真正权限 / Approval
        ↓
acknowledgeMessage
        ↓
resumeTask
```

Task Domain 不拥有真正 Approval Grant，只保存“当前 Task 被什么原因阻塞”。

---

# 15. TaskGroup / 一夜迭代

```text
TaskGroup READY
    ↓ 人工一次明确启动
TaskGroup ACTIVE

Task-01 READY
    ↓ startTask
ACTIVE → ... → SUCCEEDED

Task-02:
前序满足 → READY
扩展 / 自动化可继续调用 startTask

若任一 Task:
WAITING / FAILED / PAUSED
→ 整条链不继续释放后续 Task
→ 等人工处理
```

Task Domain 只负责 eligibility / status / sequence / maxActiveTasks，不负责打开浏览器或启动 GPT Conversation。
