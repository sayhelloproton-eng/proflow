---
docId: TASK-DOC-05-01
title: 任务与编排领域｜技术选型与工程取舍
docType: decision-rationale
authority: explanatory
lifecycle: active
domain: task-orchestration
subdomain: null
subdomains: []
provides: []
requires: []
contractRefs: []
---

# 任务与编排领域｜技术选型与工程取舍

> 本文记录“为什么选当前方案”，避免后续在没有新需求的情况下重复横向选型。

---

# 1. 当前工程硬约束

当前 ProFlow 实施基线：

```text
MacBook Pro 2017 Intel
8GB RAM
macOS Ventura 13.7.8
Node.js 24.19.0
pnpm 11.21.0
TypeScript 7.0.2
ESM only
```

因此第一版技术选型优先：

```text
少常驻进程
少外部基础设施
无需 Docker
无需 Redis
无需独立数据库 Server
Node.js 原生能力优先
TypeScript 主栈
```

# 2. SQLite：选择

选择原因：

```text
单机第一版足够
无独立 Server
事务满足 Task/Node 强一致
适合 repo-local state
低内存 / 低运维
可直接备份 / 诊断
```

第一版：

```text
SQLite + WAL + foreign_keys
```

---

# 3. PostgreSQL：不选第一版

PostgreSQL 对复杂关系、并发和未来多机很好，但当前会增加：数据库服务安装、启动、账号 / 端口 / 数据目录管理、额外 daemon 和部署复杂度。

当前没有多机 Task scheduler / 高并发需求，所以不进入 v1。

未来触发：

```text
多进程高并发
多机调度
SQLite 写并发成为真实瓶颈
跨实例共享 Task truth
```

再评估。

---

# 4. `node:sqlite`：选择

Node.js 24.19.0 已提供原生 `node:sqlite`。

第一版直接采用：

```text
node:sqlite
+ raw SQL
+ prepared statements
+ transaction
+ WAL
```

不再为了 SQLite 访问引入第三方 native driver。

# 5. `sqlite3` / `better-sqlite3`：当前不选

当前 `node:sqlite` 已满足 ProFlow v1 的本地 SQLite 访问需求。

没有真实能力缺口前，不引入：

```text
sqlite3
better-sqlite3
```

若未来出现 `node:sqlite` 无法满足的稳定性、性能或功能证据，再重新评估。

# 6. ORM / Query Builder：不选第一版

评估过 Kysely / Drizzle 等方向。

当前只有约 8 张业务表，SQL 简单、事务语义重要。

选择：

```text
Repository Ports
+
raw SQL implementation
```

不是让业务 Service 到处散 SQL。

未来真实出现几十张表 / 复杂查询 / SQL 重复维护成本后再评估。

---

# 7. XState：不选第一版

XState 适合状态机表达，但当前 v1 已收敛为简单状态 + 显式领域动作：

```text
start
complete
wait
fail
pause
resume
reopen
terminate
```

直接 TypeScript `assertTransitionAllowed()` 更透明。

未来复杂 DAG / 并行 / statechart 需求真实出现再评估。

---

# 8. DBOS：不选第一版

DBOS TypeScript 的 durable execution / Postgres workflow 思路具有可借鉴点，但当前：

```text
Task 是业务状态真源
Workflow 是数据定义的有序 Node
Agent / Extension 外部驱动
本机要求极轻
```

引入 DBOS 会增加运行模型重叠，v1 不需要。

---

# 9. Temporal：不选第一版

Temporal 对 durable workflow 非常成熟，但需要独立 Runtime / Service 体系，当前单机第一版明显过重。

保留其 Workflow vs Activity 的领域边界启发，不复用其运行基础设施。

---

# 10. BullMQ / Redis：不选

当前没有独立异步 Worker Queue 真实需求；BullMQ 会引入 Redis。

Node 由扩展 / Agent 明确驱动，不需要先建立队列。

---

# 11. pg-boss / Graphile Worker：不选当前版本

Postgres-backed queue 思路有价值，但仍要求 PostgreSQL，并且调研时最新 Node 要求与当前 Node 24.19.0 基线不友好。

当前没有队列需求，因此不为它改变基础架构。

---

# 12. Trigger.dev / Vercel Workflow：不嵌入，仅吸收适用设计原则

它们在 Run / Attempt / Wait / Durable Workflow 上有可借鉴设计，但主要面向 code-defined workflow / 平台运行时，不作为当前实现依赖。

本项目 v1 的 Plan 是 Task-owned data-defined ordered Nodes，且大量推进由 Agent / Extension 通过 Public API 驱动，因此只借鉴概念，不嵌入。

---

# 13. Hatchet 等独立工作流服务：不选

会引入额外服务 / 非 TypeScript 主 Runtime / 运维复杂度，当前无必要。

---

# 14. Runtime Validation：必须；具体库暂不冻结

公共边界必须遵循：

```text
unknown
→ runtime validation
→ typed value
```

JSON Schema Draft 2020-12 仍可作为机器可读公共 Contract 表达。

当前不预先冻结 Ajv / Zod / TypeBox / Valibot 等具体运行时校验库；在首个真实边界实现出现时，依据实际需求做最小选型。

内部可信对象不重复做无意义校验。

# 15. Node 原生 HTTP：选择

ProFlow v1 HTTP transport 直接使用：

```text
node:http
```

Domain / Application Service 不依赖 HTTP framework。

当前不引入 NestJS / Hono / Fastify。只有真实出现大量路由、中间件、统一认证/限流/插件等复杂度后才重新评估。

# 16. Git / Markdown：选择

Requirement / PRD / 技术方案 / 测试结果天然需要：

```text
人可读
AI 可读
Git diff
Git history
仓库统一管理
```

所以正文使用 Markdown，数据库只管索引和运行事实。

---

# 17. CodeGraph：可选研发工具，不是 Task 存储

若未来安装 CodeGraph，可用于研发 Agent 做代码结构 / 调用图查询。

其 `.codegraph` 数据是可重建索引，不属于 Task Domain 业务真源。

---

# 18. 当前实施技术组合

```text
Node.js 24.19.0
TypeScript 7.0.2
pnpm 11.21.0
ESM only
SQLite
node:sqlite
raw SQL
Runtime validation required at boundaries; library deferred
node:http
Markdown + Git
node:test + node:assert/strict
Biome 2.5.6
```

没有新真实需求，不重新横向选型。
