// Bootstrap placeholder.
//
// Provides a single typecheck input for `tsc --noEmit` while ProFlow remains
// in bootstrap state (no Phase 3 modules yet). Phase 3 packages will land
// under packages/ once the frozen DDD/SDD and Test Plan artifacts are imported.
export {};
