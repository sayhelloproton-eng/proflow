{
	"name": "proflow",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"packageManager": "pnpm@11.21.0",
	"engines": {
		"node": "24.19.0"
	},
	"scripts": {
		"lint": "biome check .",
		"format": "biome format --write .",
		"format:check": "biome format .",
		"typecheck": "tsc --noEmit",
		"build": "node scripts/build-packages.mjs",
		"test": "pnpm build && node --test 'packages/*/tests/**/*.test.ts'",
		"architecture": "node packages/deployment-conformance/src/architecture-cli.ts",
		"publishability": "node scripts/publishability.mjs",
		"check": "pnpm build && pnpm lint && pnpm typecheck && pnpm test && pnpm architecture && pnpm publishability"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.6",
		"@types/node": "24.10.1",
		"typescript": "7.0.2"
	}
}
