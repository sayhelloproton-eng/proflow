# @tomflow/proflow-execution-runtime

Durable, policy-controlled Execution Core orchestration over SQLite. Browser work is accepted only through an injected executor port; no Browser Extension implementation lives here.
