# @tomflow/proflow-agent-runtime — Module Setup

## STEP-AGENT-RUNTIME-01 — 完成模块自动配置

Responsible: AI
Interactive executable: `platform setup --module agent-runtime`
Non-interactive executable: `platform setup --module agent-runtime`
Required inputs: none
Verify: `platform status`
Success condition: `agent-runtime.setupStatus=READY`.
