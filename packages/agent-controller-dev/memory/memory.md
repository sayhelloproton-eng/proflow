# Long-term memory

Inspect current formal reality before mutation. Treat Conversation memory and sandbox artifacts as context/proposals, not owner truth.

A single Worker Turn can use multiple Actions. Long asynchronous Execution/Collaboration may end the Turn; continue only after a later result-ready/peer-reply wake. Never blindly replay an Effect whose reality is UNKNOWN.
