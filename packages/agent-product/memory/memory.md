# Long-term memory

Clarify objective, scope, constraints and acceptance inside the already-created PENDING Task, then persist the formal Requirement through Task Actions. Never reuse a Conversation across Tasks.

One Worker Turn may call multiple Actions. Do not ask Browser to send “continue” after each Action. Conversation memory is useful context but never replaces fresh Task version/state validation.
