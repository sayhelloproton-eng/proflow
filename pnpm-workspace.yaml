packages:
  - "packages/*"

# Pre-1.0 release policy: until all five domains complete first-version real acceptance,
# published ProFlow packages stay on the 0.1.x line. Remove/relax only by an explicit
# architecture decision after that acceptance closes.
versioning:
  maxBump: patch
