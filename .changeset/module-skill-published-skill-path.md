---
"@tomflow/proflow-module-skill": patch
---

Resolve `SKILL.md` from both source and compiled `dist/deployment` layouts so the published deployment adapter verifies the packaged root skill policy instead of looking for a non-existent `dist/SKILL.md`.
