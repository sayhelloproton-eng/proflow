---
"@tomflow/proflow-platform-cli": patch
---

Clean pnpm release-age and empty-root lockfile artifacts after whole Workspace uninstall so Deployment-owned package-manager changes do not remain behind.
