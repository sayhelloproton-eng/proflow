## 0.1.24

### Patch Changes

- Keep an existing verified FAST/THINK mapping when provider inventory changes only by adding or removing unrelated models, while still remapping when a mapped role model disappears or the user explicitly selects a replacement.
